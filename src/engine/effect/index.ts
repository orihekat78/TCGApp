// engine.effect namespace barrel
// spec: .claude/specs/engine-api-effect-descriptor.md
// Phase 3 Group A: runAtom のみ。run / seq / choose 等は後続 Group で追加。

export { runAtom } from './atom-handlers.js';

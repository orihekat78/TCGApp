# D08025 蘭の一撃 (イベント)

## 公式

no D08025 | イベント | 青 | Lv5
> 【パートナー青】（パートナーが条件を満たしている場合、この能力か効果を使える）AP8000以下のキャラを1枚まで選び、リムーブする。

## abilities

### a1 メイン (使用時): AP8000以下リムーブ

- type: triggered (event 使用), trigger: `{ hook:'effect:declared', matcher: ... }`
- condition: `{ kind:'partnerColor', color:'青' }`
- scope: on-hand
- effect:
  ```typescript
  { kind:'choice', chooser:'self', options:[
    { kind:'atom', verb:'sceneRemove', args:{ uid:'$pick', cause:'effect',
      target:{ kind:'pick', query:{ area:'scene', side:'either', filter:{ apMax:8000 } },
        n:{ min:0, max:1 }, chooser:'self' } } } ] }
  ```

## patterns

- **eventRemoveByAP** (D08025, D11020 部分): イベントで AP上限指定リムーブ — 共通候補

## エッジ

- パートナー青でない: 「能力/効果を持っていない扱い」(rules/17) → 使えるが何も起こらない
- 対象0枚: 0枚選択可 (rules/15) → スキップ
- 自分のキャラも対象可 (side:'either')
- 使用後リムーブエリアへ (engine 自動)

## 関連

- 共通候補: cards/_shared/eventRemoveByAP.ts({ apMax: 8000, side:'either' })

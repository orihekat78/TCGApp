# B04048/B04048P 羽田秀𠮷 grounding (2026-07-10, fable)
> 状態: DEFER (SMALL_GAP 2点 — drawUpToHandSize bind + handToDeckBottom dyn-n/shuffle)

## engine gap
- a1 のみ gap。DEFER 行「引いた枚数の bind-return が必要 → 別 variant DEFER」は現行も正:
  1. `atomDrawUpToHandSize` (`src/engine/effect/atom-handlers/core.ts:36-50`) は
     `need` を計算するが **bind を書かない** → 「引いた枚数」を後段が参照不可。
     fix: `if (typeof a.bind==='string' && drawn.length>0) ctx.bindings[a.bind]=drawn.map(cardId=>({cardId}))`
     (mill core.ts:292-294 と同流儀。mutate.deck.draw の戻値 or hand 差分で cardId 取得、~3 行)。
  2. `handToDeckBottom` 短縮形 pick の n を dyn 化 (「引いた枚数と同じ数」= n:{dyn:'$bound.$drawn.count'})。
     `hdArgs`/buildShortFormPick は literal n 前提 (core.ts:113-121) — handler 冒頭で
     resolveDeltaToNumber 同型の local 解決 (evidenceFlip dyn-max core.ts:536 同型)。
     `$bound.<key>.count` dyn は出荷済 (`src/engine/dyn/eval.ts:529-530`)。
  3. 「**シャッフルして**デッキの下に移す」— hdMove は選択順 push (core.ts:124-133)。
     `shuffleMoved:true` arg (moved を shuffle して bottom push、~3 行) を同 handler 修正に同梱。
- a2 は **gap 無し** (DEFER 未収載が正しい): declareName + deckRevealUntil の filter dyn
  cardName は出荷済 — `$declared.<key>` 文字列 dyn (`src/engine/dyn/eval.ts:566-582`、
  filter 用と明記) + deckRevealUntil dispatch 時 resolveFilterDynObj (picks.ts:11-19、W5 r47)。

## DSL 案
- a1 (triggered enter selfOnly, condition:{partnerColor:'赤'}): sequence[
  `drawUpToHandSize{player:'self', n:7, bind:'$drawn'}` ← 要 fix1,
  `handToDeckBottom{player:'self', n:{dyn:'$bound.$drawn.count'}, shuffleMoved:true}` ← 要 fix2/3
  ] — 手札7枚以上なら drawn=0 → n=0 pick は no-op (Q&A「実質何も起こらない」一致)。
- a2 (declared, scope:'on-scene', limit turn1, cost 無し): chain[
  `declareName{bind:'named'}` (B09108 a2 同型、DeclareCardNameModal 配線済),
  `deckRevealUntil{player:'self', maxN:2, chooseMatch:'upTo',
    filter:{cardName:{dyn:'$declared.named'}}, bind:'$revealed', bindMatch:'$matched'}`,
  conditional{if:{kind:'bound', key:'$matched', presence:'matched'},
    then: `handAddFromDeck{player:'self', cardId:'$matched.cardId'}`},
  `deckToBottomBound{player:'self', bindKey:'$revealed'}` ]
  — 分割名 rule (rules/19) は cardName filter 側の既存 components 照合に委譲。

## tier + 理由
- **SMALL_GAP** — additive 2 点 (どちらも handler-local、既存カード未使用 arg = 回帰0)。
  a2 は B09108 a2 + D01013 a1 の合成 clone で GREEN。

## 罠
- deck 残 < 7-hand の draw は mutate.deck.draw が refresh 込みで処理 — bind は refresh 跨ぎでも
  cardId snapshot なので count は正しい。
- 「シャッフルしてデッキの下」は移すカード群の順序無作為化 (デッキ全体 shuffle ではない —
  B05092 の shuffleThenDrawMoved とは別物。取り違え注意)。
- a2 の宣言名は Q&A 上プレイヤー自由入力 (存在カード名) — DeclareCardNameModal の既存 UX に従う。
- deckRevealUntil maxN:2 は「見る」= 非公開情報の閲覧、公開は加える 1 枚のみ — 既存 overlay
  (PendingDeckRevealSide) の見せ方をそのまま流用 (D01013 と同じ)。

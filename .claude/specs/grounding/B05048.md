# B05048 grounding

- Printed: while bonded to `中森青子`, friendly named `中森青子` cannot be selected by opponent abilities/effects; action trigger enters up to one white level-5-or-less character from remove; hirameki returns up to one `中森青子` from remove.
- Mapping: conditional `untargetableByOppEffectAura`; turn-limited `action:declare` remove `sceneEnter`; optional evidence-remove `handAddFromRemove`.
- Q&A: action target selection is not blocked; switch and entering-character triggers remain normal.

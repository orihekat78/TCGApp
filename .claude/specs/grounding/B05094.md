# B05094 grounding

- Authority: release packet `191a8c7bfae2f027995030322f081837c44d8fcc`, source digest
  `8409a76a2b33dcd65f339b97741ae2e0eaba2fd7175ecd789dc5b9e5e72137c9`,
  `ct-p05/character.tsv` SHA-256 `f9fd90bcd2b0ea41fc7274cde0114f6e4109c92c82f7972ebef7f216e4018074`.
- Printed leave ability: during the opponent turn, when this scene character goes to remove, reveal from
  the top until the first `長野県警` character, add it to hand, move the other revealed cards to deck bottom,
  then shuffle the deck.
- Official Q&A: the first matching character is mandatory. With no match, return every revealed card and
  shuffle. The trigger requires this physical scene character to leave during the opponent turn.
- DSL: `leave:to-remove/selfOnly` + opponent-turn condition -> public/all `deckRevealUntil` filtered by
  `trait:'長野県警'` and `kind:'character'` -> conditional exact hand add -> preserved bottom move -> shuffle.
- Fix: `order:'preserve'` removes the unsupported human bottom-order decision.
- Verification: public positive/no-match, same-trait event decoy, forced acquisition, no reorder,
  timing/selfOnly negatives, and terminal cleanup.


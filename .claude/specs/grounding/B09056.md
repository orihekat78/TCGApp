# B09056 赤井秀一 grounding

Source: pinned CT-P09 character TSV
(`34f2babbaaf07cef0f19ff7a765ca7052262d7c43637230b606b14306ff20c04`).

## Wave55 choice and nested entry

- Printed a1 optionally sleeps self and removes up to one level-8 character,
  then always presents two trace-labelled choices.
- Official QA permits choosing the inapplicable branch for no effect. The old
  deterministic trace conditional was wrong; B09056/P now use a real choice
  whose options conditionally resolve found-entry or unfound-mill.
- Autonomous resolution skips only top-level conditional options known false
  and lacking an `else` branch;
  unconditional choices retain index-0 behavior, and opponent-owned choices do
  not leak to the human side.
- Public flow proves zero removal still reaches choice, invalid branch no-op,
  typed sleeping entry, and the nested entrant's normal enter ability.

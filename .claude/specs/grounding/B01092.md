# B01092 松田陣平 grounding (2026-07-10, sonnet5)
> 状態: DEFER (T3、★LOUD — 既存メモ通り stale ではない)

## 印字 (要約)
【相手ターン中】自分の現場のこのキャラ以外のキャラ1枚が相手の能力/効果/コンタクトによって
現場から離れるとき、このキャラをリムーブしてもよい。そうした場合、そのキャラは現場から離れる
代わりに手札に移す。【ヒラメキ】カードを1枚引く。

## engine gap（実在、consult-leave-intercept.ts 直読で確認）
機構本体（pre-splice consult）は shipped（mega-wave W6 step10）:
`consultLeaveIntercept`(src/engine/effect/consult-leave-intercept.ts:48-111) が
帰属判定 (`cause==='contact-ap'` or 相手 byPlayer の effect) → optional interceptor 走査まで実装済み。
**しかし L87 `if (getHumanPlayerSide() === player) return null;` により、B01092 の所有者が human
の場合は無条件に intercept を skip（素通し＝通常リムーブ）する。** human は「してもよい」の
選択を一切提示されない。AI 所有側のみ heuristic（在場なら常時 accept, L105-107）で機能する。
コード内コメント（L20-22）で「human-defender の accept/decline window は別 increment に
明示 DEFER」と明記されており、本 DEFER note は stale ではなく現状と完全一致。

## DSL 案
primitive レベルの gap ではなく **flow/UI 機能の欠落**（interrupt-style 選択ウィンドウ）。
既存の `action:guard-window` hook（相手ターン中断→ガード選択→再開）と同型の
`leave-intercept-window` フェーズを state-machine に追加し、`removeToRemove` の splice 前に
human 所有時のみ一時停止 → accept/decline を UI から受け取って `consultLeaveIntercept` の
分岐に反映する設計が必要。

## tier + 理由
T3。相手ターン中の割込み UI ウィンドウ + hot-path（removeToRemove splice）への介入 = flow core 変更。

## 罠
- 「human-defender window」というメモの表現は正確 — 実装は AI-only で human 側は未対応のまま。
- human vs CPU で B01092 の**所有者が CPU** なら現状でも一応機能する（accept heuristic）が、
  「所有者が human」の主用途（このカードは相手ターン中の受け身能力なので人間プレイヤーが
  操作したいケースが主）は本 gap がある限り機能しない。card author 前に必須実装（既存メモ通り）。

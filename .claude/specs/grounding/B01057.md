# B01057 「最も出会いたくない…恋人ってトコロかな？」 grounding (2026-07-10, fable)
> 状態: DEFER (SMALL_GAP, T2) — DEFER 行 3 blocker のうち 2 つは解消済 (stale)、残 1 点のみ

## engine gap (DEFER 行の再検証)

1. **event-self-set — ✅ STALE (解消済)**。charSetCard `fromSelf:true` 短縮形 pick が実装済で、
   コメントに **B01057【白】が名指し** されている (src/engine/effect/atom-handlers/char.ts:226-227, 313-327)。
   faceUp=true 固定 (char.ts:324) — rider READ の前提と一致。shipped consumer: B01023/B02013/B05041 ほか計9枚。
2. **set-card→host triggered 能力付与 gate — ✅ STALE (解消済)**。scope:'on-set-host' triggered rider は
   handleHook が host の faceUp setCards def を走査して合算する (src/engine/listeners/triggered.ts:271-287)。
   granted-ability validator (validate.ts:191-199) は **charGrantAbility 専用** — 印字 on-set-host rider は
   検査対象外で棄却されない。
3. **rider の leave:to-remove 不発 — 現存 (唯一の残 gap)**。host リムーブ時の順序 =
   setCards→remove push (mutate/scene.ts:294-295) → host splice (:311-313) → leave:to-remove emit (:317-337)。
   emit 時点で host は in-play scan 外 → virtual handler `handleLeaveToRemoveSelf`
   (src/engine/listeners/triggered.ts:639-691) が処理するが、**def.abilities (host 印字) のみ走査**し
   payload.removedChar.setCards の rider を見ない。card-def.ts:41-42 が gap (2) として明記 (記述は現行と一致)。
   → additive: handleLeaveToRemoveSelf に removedChar snapshot (scene.ts:334 で payload に同梱済、
   splice 前 ref なので setCards 保持) の faceUp setCards を走査し、set card def の
   scope:'on-set-host' + hook:'leave:to-remove' triggered を同流儀で queue する loop を追加。
   source は host (uid/cardId/player)、condition/matcherCondition は既存 evalCond 経路をそのまま通す。
   Q&A「2枚セット→2つ発動」= entry ごとの loop で構造的に成立。

## DSL 案 (全句)

- a1 (使用効果): trigger{hook:'effect:declared', selfOnly, matcher kind==='event-use'} (B01023 a1 同型) →
  `charSetCard{player:'self', fromSelf:true, n:1, filter:{color:'白'}}` (「【白】のキャラ1枚に」
  — char.ts:227 が本カードの filter 例として明記)。現場0/白0 なら host-absent no-op →
  解決後リムーブ (Q&A と一致、B01023 Q&A 同文)。
- a2 (rider): `{ id:'set_t1', type:'triggered', scope:'on-set-host',
    trigger:{hook:'leave:to-remove', selfOnly:true}, condition:{kind:'turn', player:'opp'},
    effect: sceneEnter{from:'hand', player:'self', n:{min:0,max:1},
      filter:{kind:'character', color:'白', levelMax:7}} }`
  (【相手ターン中】【現場リムーブ時】手札からレベル7以下の【白】を1枚まで登場 — 「まで」=0枚可 rules/15)。
  selfOnly の照合は virtual handler 側で host uid = source.uid として成立させる (追加 loop の実装形に依存、
  rider は「host がリムーブされたとき」なので host-self 固定で良い)。
- a3 ヒラメキ: scope:'on-evidence' + draw 1 (多数 shipped 同型、B01023 a2 等)。

## tier + 理由

**SMALL_GAP (T2)** — 残 gap は handleLeaveToRemoveSelf への additive loop 1 点 (既存
handleDisguiseReplacedSelf:701 と同構造の走査追加)。listener core を触るため T2 (2 lens) +
vitest probe (host リムーブ→rider 発火 / 相手ターン gate / 2枚セット2回発火 / 裏向きセットは不発)。

## clone 元 (shipped)

- event self-set + on-set-host rider: **B01023 シャッフルロマンス** (fromSelf + rider continuous) /
  **B02013** (rider keyword grant)。triggered rider の shipped 例は B05041/B01039 系を確認のこと
  (leave:to-remove rider は本カードが初 = 残 gap の理由)。
- rider ability.id は card-unique に (card-def.ts:45-46 authoring hazard、'set_t1' 等)。

## 罠

- validate.ts:197 の leave:to-remove 禁止は **charGrantAbility 経路のみ**。本カードは印字 rider なので
  通るが、lint が将来 on-set-host にも拡張されたら engine fix と同 commit で解除すること。
- host が deck/hand/evidence へ離場した場合は leave:to-remove ではない → rider 不発が正
  (rules/17【現場リムーブ時】= リムーブエリアに置かれたとき。Q&A 文言と一致)。
- P 版 (B01057P) 同時出荷 (spread)。

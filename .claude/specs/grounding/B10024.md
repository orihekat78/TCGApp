# B10024 大滝悟郎 grounding

- Source: `ct-p10/character.tsv` rows B10024/B10024P; rechecked 2026-07-26 with `npm run ground -- B10024`.
- P: behavior/text are identical; only ID, number suffix, rarity, image metadata differ.
- Enter ability: partner-green gate, then optional `handReveal` of one hand character with `警察`; a chain permits the AP≤8000 removal only after a non-empty reveal. Target selection is up to one character on either scene.
- Q&A: the revealed Police character may return to hidden hand state once this effect has resolved. `handReveal` keeps zones unchanged and binds the revealed card only for this continuation.
- Declared ability: sleep this source, choose up to one self scene Police character, and `charSetCard` the self deck top face-down. A missing selected host or empty deck is a no-op; deck content is not consumed if the host vanished.
- Hirameki: optional evidence-removal trigger; choose up to one scene character and sleep it.
- Human continuation coverage: reveal → AP target and Police-host → deck-top set paths are exercised directly through pending-pick/apply-pick dispatch, including duplicate hand/deck IDs, zero reveal, vanished target/source/host, and empty deck.
- UI gap: generic hand selection can represent the reveal operation, but the client has no dedicated persistent public-reveal display/lifetime. No approximation was added; the official Q&A permits returning the card after resolution.
- Tests: `tests/cards/ct-p10/b10024.test.ts`.

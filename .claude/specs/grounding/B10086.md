# B10086 grounding

- Source: CT-P10 character.tsv and official Q&A inspected 2026-07-26.
- Needs a contact-time, named-card cut-in trigger that readies this character then forbids only its contact's later cut-ins; the printed cut-in separately draws only for a black cut-in holder.
- `cutin:used` + `triggerCutinMatches` identifies the named Bourbon cut-in. `contactCharMatches{who:byUid,requireSource:true}` keeps the trigger on this character's own contact without a non-serializable custom descriptor.
- Resolution readies the source, then grants a turn-scoped `continuous/on-scene` `selfCutinBanInContact` descriptor. The reader honors its scope and condition, survives original-ability disable, and expires with turn cleanup or bearer departure.
- The shared `canCutIn` gate blocks only the granted bearer while it is the contacting character. It does not restrict the other character, the other side, or `canDisguise`; prior granted abilities remain stacked.
- Twin printing: B10086P.

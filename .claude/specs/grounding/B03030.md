# B03030 / B03030P grounding

- Printed: bond `大岡紅葉` grants opponent-effect target protection; once each turn when this character declares an action, enter up to one level-6-or-less green character from hand asleep.
- Mapping: `bond` + `untargetableByOppEffect`; `action:declare` self trigger, turn limit, `sceneEnter` with `enterSleep`.
- Verified: action-declare remains before guard and uses normal switch/enter behavior; zero enter is legal.


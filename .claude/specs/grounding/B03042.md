# B03042 grounding

- a1: top 5 deck window. Pick 0--2 `character trait:探偵`; `distinctColors` rejects every overlapping printed color. Add picks to hand. Bound remainder → deck bottom → shuffle.
- a2: `on-evidence` `evidence:remove-by-action`, optional. Pick 0--1 self remove `character trait:探偵` → hand.
- Q&A: 0 or 1 pick legal even with candidates. Covered by `n.min:0`.

# B03042 grounding

- Source: pinned dossier `.tmp/ground-wave10-shuffled-remainder-20260816/B03042.md`.
- a1: top 5 deck window. Pick 0--2 `character trait:探偵`; `distinctColors` rejects every overlapping printed color. Add picks to hand. Shuffle only the bound remainder, then move it to deck bottom.
- DSL: `deckToBottomBound { bindKey:'$revealed', order:'shuffle' }`; no following whole-deck shuffle.
- a2: `on-evidence` `evidence:remove-by-action`, optional. Pick 0--1 self remove `character trait:探偵` → hand.
- Q&A: 0 or 1 pick legal even with candidates. Covered by `n.min:0`.

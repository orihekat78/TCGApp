# B08026 grounding

- Source: pinned dossier `.tmp/ground-wave10-shuffled-remainder-20260816/B08026.md` from validated CT-P08 character TSV.
- Printed tail: after looking at five cards, shuffle only the revealed remainder and move it to deck bottom.
- DSL: `deckToBottomBound { bindKey:'$revealed', order:'shuffle' }`; no following whole-deck shuffle.
- The untouched deck tail must retain its order; the public test pins deterministic remainder order below it.
- The separate declared `useEventFromHand` ability is unchanged.

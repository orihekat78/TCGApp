# B04055 grounding

Source: `npm run ground -- B04055`; CT-P04 character TSV and printed Q&A.

## Text mapping

- Opponent turn / turn 1: `turn:opp` and `limit:{kind:'turn',n:1}`.
- Another own red scene character removed: observer `leave:to-remove` with
  `removedCharMatches{side:self,removedFilter:{color:赤}}`.
- Amanda must remain asleep in scene: `scope:on-scene` plus
  `sourceInScene` and `charStateIs($self,sleep)`. If removed simultaneously,
  source presence is false at resolution,
  matching printed Q&A.
- Reveal top one: `deckRevealUntil{maxN:1}`.
- Same trait as removed character: `traitSharedWithTriggerRemoved:true`; it
  compares any effective snapshot trait, fails closed without snapshot.
- Match is mandatory hand add; non-match moves to deck bottom through
  `$revealed`.

## Evidence

Focused probes cover matching/nonmatching reveal, opponent/own turn, sleep,
color gate, zero snapshot fail-closed, and real remove dispatch.

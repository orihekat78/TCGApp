# B10012 真田貴大

- Source: CT-P10 TSV / official Q&A, dossier `.tmp/_ground/B10012.md`.
- Printed clauses: partner blue + enter → self deck top face-down set; declared sleep → move one own hidden set card to an own Soccer player with no hidden set; turn-1 own hidden set removal → self gains Rush for the turn.
- DSL: `charSetCard fromDeckTop`, `bindPick` + `moveSetCard`, and `charGrantKeyword` respectively.
- Q&A: face-down set identities stay hidden. Tests cover hidden/duplicate/stale moves through `move-set-card.test.ts`.

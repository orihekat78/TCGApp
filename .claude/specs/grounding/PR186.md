# PR186 grounding

Source: current-parser PR-01 character TSV rederived from the retained API snapshot;
see [authority manifest](waves64-65-authority.md)
(`3d4feccc677b7d4df75498f9e872c743cc94236d0e1938d9ec01928d7e5651cf`).

- `CardDef.entersSleep` makes every entry route create the source asleep before
  enter hooks. No synthetic sleep ability remains in the ability array.
- Search a2 alone opens the top-three window at printed ability index 0. Selection
  publishes the sole FBI card, refreshes, and then reaches the gated discard.
- A coherent legacy pending entry/runtime pair at index 1 still hydrates and
  completes; mixed legacy/fresh indices reject transactionally.

## Wave85 exact ruling reauthentication

- Public effect entry confirms sleep inside synchronous `enter`, no false
  `state:change`, and exactly one genuine a2 search window.

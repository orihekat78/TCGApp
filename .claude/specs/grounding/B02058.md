# B02058 / B02058P grounding

- Authority: release packet `191a8c7bfae2f027995030322f081837c44d8fcc`, source digest
  `8409a76a2b33dcd65f339b97741ae2e0eaba2fd7175ecd789dc5b9e5e72137c9`,
  `ct-p02/character.tsv` SHA-256 `5773e343d972092e612b4d0ac299b663e3153b2b40fbd01a130ebccfaca653ab`.
- Printed leave ability: during the opponent turn, when this scene character goes to remove, reveal from
  the top until the first card named `沖矢昴`, add it to hand, move the other revealed cards to deck bottom,
  then shuffle the deck.
- Official Q&A: the first match is mandatory. With no match, return every revealed card and shuffle.
  The trigger requires this physical scene character to leave during the opponent turn.
- DSL: `leave:to-remove/selfOnly` + opponent-turn condition -> public/all `deckRevealUntil` -> conditional
  exact `handAddFromDeck` -> `deckToBottomBound(order:'preserve')` -> whole `deckShuffle`.
- Fix: `order:'preserve'` removes the unsupported human bottom-order decision. The final shuffle remains.
- Verification: public positive/no-match for B02058, exact-name filter, forced acquisition, no reorder,
  timing/selfOnly negatives, duplicate occurrence, owner mirror, and short-deck refresh.


# B10023 grounding

- Source: pinned CT-P10 character TSV
  (`c8592b91ab6281829f40ff2018d32c0493f62ca424483f60edaf5ced886a6d8d`).
- Entry chain is optional only after self hand count is at least one: sleep source, remove one hand card, then optionally enter a level-6-or-less green police character from remove.
- Declared ability uses `removeSetCard { n: 2 }`: any two face-down set cards across the owner's entire scene, then draws one card.
- B10023P is mechanically identical. Regression: `tests/cards/ct-p10/b10023-b10024.test.ts`.
- Wave55 public hand use distinguishes the outer B10023 enter from its nested
  green Police effect-entry. Sleep/discard complete before the entrant's normal
  enter ability; no post-use hand exposes no optional or false entry.

## Wave61 full-scene effect entry

- B10023/P may sleep in the fifth slot, discard one exact hand occurrence,
  effect-enter a green Police character from remove, and switch out that source.
- Source departure does not refund the discard or suppress the nested enter hook.
  Zero entry retains the paid discard; optional decline preserves every cost.

## Wave62 face-down set privacy

- B10023/P a2 removes two exact owner hidden set occurrences. Replay projections
  expose only backs/count, and the real declared-cost UI request carries hidden
  occurrence IDs without either `cardId`.
- Insufficient payment rejects transactionally. Successful payment reveals the
  two identities only after they reach owner remove.

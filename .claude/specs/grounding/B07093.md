# B07093 / B07093P バーボン＆ライ grounding

Source: SHA-pinned CT-P07 character TSV dossier, 2026-08-23.

## Text mapping

- Printed `a1` is black-partner, FILE7, declared, turn one, then a true choice
  between up-to-one level-4-or-lower Black Organization entry from hand/remove.
- Each branch keeps its own `$matched` binding, then grants AP+4000, Assault,
  and `toDeckBottomOnTurnEnd` to that physical entered character.
- FILE counts the assisted partner; zero is legal; effect entry fires `enter`;
  a full scene permits switch, including removal of B07093 itself.
- Disguise retains AP, Assault, and the rider. Early leave clears the rider with
  that character; otherwise end turn moves its current card to exact deck bottom.
- Existing `[a2,a3]` physical indices are save/replay authority, so `a1` is
  appended as index 2. Base and parallel definitions remain mechanically equal.
- Engine gap: none. Tier T2, missing printed-definition completion.

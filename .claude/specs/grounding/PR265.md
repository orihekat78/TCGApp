# PR265 風見裕也 grounding (2026-07-10, fable)
> 状態: DEFER (SMALL_GAP 1点 — mill n の dyn 解決)

## engine gap
- **唯一の gap**: 「そのカードのレベルと同じ枚数リムーブする」= mill n を bound カードの印字 level
  で動的化。`atomMill` は `const millN = a.n as number` (`src/engine/effect/atom-handlers/core.ts:277`)
  で literal のみ。dyn 値 (`$bound.$matched.level`) は評価不可。
- **fix**: handler-local `resolveDeltaToNumber(a.n, s, ctx)` 化 — souza x:{dyn} と完全同型
  (`src/engine/effect/atom-handlers/picks.ts:377-381`、number 素通り = 既存 literal 消費者 byte 互換)。
  pre-walk (resolveDynArgs) 依存は不可 — chain 前段 bind は実行時確定 (walk-literalize 罠、
  memory reference-miniwave-loop-and-pick-dsl-rules)。
- dyn 側は既存: `$bound.<key>.level` = 先頭要素 cardId → def.card(cardId).level
  (`src/engine/dyn/eval.ts:541-550`)。matched は deck 由来 Candidate{cardId} → 印字 level ✓。
- DEFER 行「dynamic-count (bound-card-level) mill (engine)」は正確 — 現行も未解消、ただし
  必要工事は上記 1 行 + probe のみに縮小済 (resolveDeltaToNumber/levelSum 基盤が W5 以降出荷済)。

## DSL 案
- a1 (triggered enter selfOnly): sequence[
  `deckRevealUntil{player:'self', maxN:1, chooseMatch:'upTo',
    filter:{trait:'警視庁', kind:'character'}, bind:'$revealed', bindMatch:'$matched'}`,
  conditional{if:{kind:'bound', key:'$matched', presence:'matched'}, then: sequence[
    `handAddFromDeck{player:'self', cardId:'$matched.cardId'}`,
    `mill{player:'self', n:{dyn:'$bound.$matched.level'}}`  ← 要 gap fix ]},
  `deckToBottomBound{player:'self', bindKey:'$revealed'}` ]
  — D01013 a1 の maxN:1 clone + mill 差替。「1枚まで公開して」= chooseMatch:'upTo' (加えない選択可、
  Q&A 一致)。「残りを好きな順番でデッキの下」= deckToBottomBound (maxN:1 なら残り高々1枚)。
- a2 (declared, scope:'on-scene', 【スリープ】+〚手札1枚リムーブ〛):
  - condition: `{caseStatus:'解決編'}`
  - cost: `{kind:'pay', items:[{kind:'sleepSelf'}, {kind:'removeFromHand', target:{pick hand self},
    n:1}]}` (B07079 a2 cost 同型)
  - effect: `sceneRemove{player:'self', side:'either', max:1, filter:{levelMax:5, kind:'character'}}`

## tier + 理由
- **SMALL_GAP** — additive 1 点 (core.ts:277 の n 解決を resolveDeltaToNumber 化、~1 行 + probe)。
  他全句は D01013 (deck-look) / B07079 (宣言 cost) の既存 idiom clone。

## 罠
- mill dyn は必ず handler-local で解決 (walk-literalize: pre-walk 時点で $matched 未確定 → NaN)。
- mill の refresh 挙動は Q&A と一致済 (core.ts:295-303 可能な限り→refresh→残り分リムーブしない)。
- 手札に加えた時点でデッキ0 → refresh 後に mill が走る順序 (rules/26) — probe で deck1 ケース確認。

# B10029 grounding

- Official source: `ct-p10/character.tsv`, card B10029.
- Printed clause: on entry, if own scene has `服部平次` or `遠山和葉`, may sleep self; if so add up to one green event from own remove to hand.
- Mapping: self `enter` trigger; `sceneHas` card-name OR condition; `optional` wrapping `chain(sleep self, handAddFromRemove)`.
- `chain` preserves the printed 「そうした場合」 causal gate.
- Boundary: no named character means no optional prompt; no eligible event permits the zero-card choice.
- Engine change: none.

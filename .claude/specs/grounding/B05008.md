# B05008 / B05008P grounding

- Printed: bond `江戸川コナン` grants opponent-effect target protection; resolved-case declared ability grants `突撃[キャラ]` to one other `少年探偵団` character for the turn; hirameki sleeps up to one character.
- Mapping: `bond` + `untargetableByOppEffect`; case-status declared `charGrantKeyword` with `excludeSelf`; optional evidence-remove trigger and scene-state pick.
- Verified: self is excluded from the grant; pick remains optional.


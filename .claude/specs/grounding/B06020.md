# B06020 佐々木小次郎 grounding (2026-07-10, sonnet5)
> 状態: DEFER (T3、a2 単独は GREEN 相当)

## 印字 (要約)
a1【自分ターン中】自分の手札にある【緑】〚特徴[YAIBA]〛のキャラは「【カットイン】AP+2000」を
持つ（【カットイン】2つ以上なら1つ選択）。
a2【宣言】【スリープ】〚デッキ上から3枚リムーブ〛：相手の現場のキャラを1枚まで選び、
このキャラとのコンタクトを発生させる（このキャラがアクションした側）。

## engine gap
- **a2: CLEAN（全部品出荷済）**。`startContact` atom verb は登録済み
  （src/engine/effect/atom-handlers.ts:244、コメントで「W6 step9」出荷済と確認）。
  必要コスト（sleepSelf, removeDeckTop）も Cost union に既存。
- **a1: 実在の gap**。`AbilityScope`(types/card-def.ts:26-48) の `'on-hand'` は「そのカード自身が
  手札にある間ability を持つ」用途（自己完結カットイン等）であり、**盤面キャラが手札の他カードへ
  継続的にキーワードを付与するアウラ機構は存在しない**。既存の ContinuousModifier / aura 系
  （apDeltaAura*、grantKeywords 等）は grep 確認済みで `state.players[p].scene` のみ走査し、
  `state.players[p].hand` を走査する consumer は皆無（`handGrantKeyword` 等の grep 0件）。

## DSL 案
a2 (GREEN、出荷可): `declared{cost:[sleepSelf, removeDeckTop{n:3}], effect: pick(scene,side:opp,n:'0-1') → startContact{attacker:$self, target:$pick}}`。
a1 (新規機構): 継続 `{kind:'handAura', filter:{color:'緑',trait:['YAIBA']}, grantKeywords:['カットイン AP+2000']}`
を新設し、hand-card の cutin 候補列挙箇所（flow/contact.ts の cutIn 候補収集）で
`read.hand.auraKeywords(state, player, cardId)` を新規 consult。

## tier + 理由
T3（a1）。hand-zone aura consumer は新規カテゴリで、コンタクト中の cutin 候補列挙という
hot-path/flow-core への配線を要する。a2 単体なら T1 だが、project の partial-ship 禁止
運用によりカード全体は a1 解消まで DEFER。

## 罠
- DEFER note が明記する通り、旧来の非公式 triage は本カードを「cutin-subtype filter」問題と
  誤分類していた。dossier の再 triage（「手札 zone への continuous aura 付与」）が正しい —
  cutin-subtype gate は存在せず、真因は hand-zone aura consumer の不在。この誤分類の教訓は
  横展開価値あり（他の手札系 continuous grant カードも同様の誤 triage リスク）。

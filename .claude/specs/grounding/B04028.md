# B04028 grounding

- Its Hirameki is an optional `evidence:remove-by-action` triggered ability.
- Both face-up and face-down evidence are eligible when removed by Action [Case].
- Suppression prevents the prompt; a card without Hirameki creates none; optional fire/skip stays user-owned.
- Regression uses `removeOpponentEvidenceTop`, not a synthetic listener-only emit.

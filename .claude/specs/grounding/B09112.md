# B09112 grounding

Source: pinned official CT-P09 character TSV
(`34f2babbaaf07cef0f19ff7a765ca7052262d7c43637230b606b14306ff20c04`).

## Printed mapping

- Resolved-case a2 is turn one and costs exactly two own face-down evidence.
- It declares a name, derives the look count from matching own scene characters,
  may add one matching character, and moves the remainder to deck bottom.
- Dispatch-time dynamic `maxN` and named filtering are shipped; the older
  deferred note is obsolete.

## Wave53 public cost certification

- `[down,up]` rejects with exact `not-allowed`, unchanged GameState/reference,
  no decisions, and turn use zero.
- Adding one down evidence then choosing nonadjacent indices 0/2 succeeds in the
  same turn. The accepted route proves the named deck window independently.

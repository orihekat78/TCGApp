# B05080 timing grounding

## Official-QA constraint

- The trigger is created after the reasoning character is put to sleep, before
  either mislead resolution or evidence gain.
- Hash-only Q&A item: `card:B05080:a25dff9f85b6486259122963ff75c389f73edef8df5bc48d827c8712c3d4b980`.

## Implemented boundary

- B05080 listens on `reasoning:after-sleep`.
- `doReasoning` emits that hook immediately after sleep, queues an engine-only
  token-bound continuation, then waits for every reaction to settle before
  mislead and evidence gain.
- B05019 uses the same window because its accepted optional sets the current
  reasoner's LP to 0; it is not retained on `reasoning:end`.
- The hook payload retains `uid` and `player`; B05080 keeps its owner-relative
  `side:'opp'` matcher.

## Regression proof

- `tests/cards/triggerchar-target-batch.test.ts` covers LP -1 evidence count,
  discard decline, self-side gate, and partner decoy.
- `tests/e2e/triggerchar-target-2026-06-06.spec.ts` covers human discard before
  mislead and evidence.

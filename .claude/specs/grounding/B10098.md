# B10098 / B10098P grounding

Source: `npm run ground -- B10098 B10098P` on 2026-07-26; CT-P10 character TSV.

| Printed clause | Encoding |
|---|---|
| 服部平次＆怪盗キッド / 緑・白 / Lv9 / AP8000 / LP2 / MR / 探偵・高校生・怪盗 | CardDef metadata; P differs only ID, rarity, image. |
| 〚突撃〛 | `keywords:['突撃']` printed keyword. |
| 【宣言】【スリープ】〚パートナーエリアに移す〛 | `pay[sleepSelf,selfToPartnerArea]`; full preflight prevents partial sleep. |
| Remove: Lv8以下・服部平次/怪盗キッド・〚突撃〛を1枚まで登場 | `sceneEnter` remove filter with `keywordFromPrintOrConditionIcon:'突撃'`; excludes ordinary grants and suffixed icons. |
| Until turn end, entered character can action active opponent characters | `charGrantAbility` with `action:pre-target` / `expandActionTargets`, `scope:'turn'`. |
| Own Lv8+ scene character contacts: AP+2000 during contact | `contact:start` matches own `aUid`/`bUid`/`guardUid`, then contact AP delta. |
| Ability works in partner area | a1/a2 `scope:'on-partner-area'`. |
| 【カットイン】AP+2000 | on-hand optional `effect:declared`, `$contact.byUid`, contact scope. |

PA cost preserves the SceneCharacter uid, state, declared count, and turn effects; clears name plus set/stacked cards to remove. It emits no new `leave:to-partner-area` hook: existing MR movement has no such hook and official timing is unresolved. `endTurn` clears PA-MR turn keys.

Checked edges: occupied PA and stale source reject before sleep; non-MR rejects; source move has no scene-leave event; UI/AI use preserved uid; printed plain keyword / condition-icon match, ordinary grant and `突撃[キャラ]` / `突撃[事件]` do not.

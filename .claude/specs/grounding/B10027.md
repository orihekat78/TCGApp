# B10027 長島茂雄

- 根拠: CT-P10 `character.tsv`、公式Q&A（裏向きセットは内容を確認不可）。
- 【パートナー緑】【宣言】【ターン1】は `partnerColor`、`declared`、turn limit。
- 裏向きセットの任意リムーブは `charRemoveSetCard` の `side: 'either'`、`faceDownOnly`。
- 実際にリムーブできた場合だけ、同じturnで自身のAP+1000と【突撃】付与。
- 0枚選択・候補なしでは `$removedSet` がmatchedにならず、修正は発生しない。

## Wave62 public privacy proof

- Pinned CT-P10 SHA-256:
  `c8592b91ab6281829f40ff2018d32c0493f62ca424483f60edaf5ced886a6d8d`.
- B10027/P pending choices expose host/occurrence authority only; hidden `cardId`
  remains absent while a face-up decoy stays visible and excluded.
- Successful removal reveals the card in remove, then applies the printed AP and
  Assault rider to the physical source.

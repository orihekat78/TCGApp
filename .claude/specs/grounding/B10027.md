# B10027 長島茂雄

- 根拠: CT-P10 `character.tsv`、公式Q&A（裏向きセットは内容を確認不可）。
- 【パートナー緑】【宣言】【ターン1】は `partnerColor`、`declared`、turn limit。
- 裏向きセットの任意リムーブは `charRemoveSetCard` の `side: 'either'`、`faceDownOnly`。
- 実際にリムーブできた場合だけ、同じturnで自身のAP+1000と【突撃】付与。
- 0枚選択・候補なしでは `$removedSet` がmatchedにならず、修正は発生しない。

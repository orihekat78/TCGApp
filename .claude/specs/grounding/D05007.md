# D05007 grounding

- Official raw: `.claude/specs/cards-data/_raw/ct-d05-api.json:283,292`.
- Trigger: during the opponent turn, when this scene character moves to remove.
- Effect: look at the top three cards, choose up to one yellow level-4-or-lower character, put it into the scene asleep, then place the remainder on the deck bottom in owner-chosen order.
- Official QA permits choosing zero even when a matching card exists.
- Engine mapping: `deckRevealUntil(chooseMatch:'upTo') -> sceneEnter(enterSleep:true) -> deckToBottomBound`.
- The looked cards and chosen bottom order are private to the owner.
- Negative cases: self turn, another character leaving, zero selection, wrong color, level 5+, non-character, stale occurrence, full-scene switch boundary.

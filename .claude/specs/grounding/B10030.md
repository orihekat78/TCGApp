# B10030 grounding

- Official source: `ct-p10/character.tsv`, card B10030.
- Printed entry clause: only during 解決編, set the top deck card face down on self.
- Mapping: self `enter`, `caseStatus:解決編`, `charSetCard` with `uid:$self`, `fromDeckTop`, `faceUp:false`.
- Printed cut-in clause: AP+2000 only when cutting in to a green `警察` character.
- Mapping: on-hand cut-in gated by `contactCharMatches(byUid, color:緑, trait:警察)` then contact-scoped AP modifier.
- Boundary: either wrong color or missing trait makes it ineligible.
- Engine change: none.

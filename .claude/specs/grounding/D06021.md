# D06021 服部平次 grounding (2026-07-10, sonnet5)
> 状態: GREEN (D06003 family clone、cutinTextIncludes premise)

D06003 と印字テキスト完全一致 (TSV 突合済: 【事件緑＆白】〚突撃〛/ 【パートナー緑】【ターン1】アクション
時「【カットイン】AP＋」持ちリムーブから手札1枚まで、qAndA 3件同文)。差分はカードNo (0167/D06021)・
imageUrl (1719469902792192.jpg) のみ。詳細根拠は [D06003.md](D06003.md) 参照 (engine gap / 罠は共通)。

## engine gap

D06003 と同一 — `TargetFilter.cutinTextIncludes?: string` (src/engine/types/effect.ts:271 keyword
field 隣) が未実装 (2026-07-10 時点 src/ 0件、Grep 確認済)。本 session の engine wave で実装予定の
premise により GREEN 扱い。

## DSL 案

a1 【事件緑＆白】〚突撃〛— GREEN、D06010 a1 の印字同文 clone (実物確認済: src/cards/ct-d06/D06010.ts
a1 が同一 continuous/caseColor/grantKeywords 構造):
```
{ type:'continuous', scope:'on-scene',
  condition:{kind:'caseColor', color:['緑','白'], combine:'and'},
  continuousModifier:{ grantKeywords: () => ['突撃'] } }
```

a2 【パートナー緑】【ターン1】このキャラがアクションしたとき、リムーブの「【カットイン】AP＋」持ち1枚まで手札へ:
```
{ type:'triggered', scope:'on-scene',
  condition:{kind:'partnerColor', color:'緑'},
  limit:{kind:'turn', n:1},
  trigger:{hook:'action:declare', selfOnly:true},
  effect:{kind:'atom', verb:'handAddFromRemove',
    args:{player:'self', max:1, filter:{cutinTextIncludes:'AP＋'}}} }
```
- handAddFromRemove 出荷済み確認 (src/engine/effect/atom-pick-spec.ts:34、defaultArea:'remove'
  mode:'PB' sourceSplice:true)

## tier + 理由

**GREEN (T1、cutinTextIncludes premise)** — D06003 で additive filter 1点が解禁される family 同時
出荷対象。D06003 の SMALL_GAP(T2) 判定を本カードが単独で追加負担することはない (filter は D06003 と共有)。
filter 実装後は printed clone として T1 (機械ゲート + probe のみ) で出荷可。

## 罠

D06003 と共通 (再掲):
- ウォッカ除外 probe 必須: remove に B01097 (cutin draw型、「AP＋」非包含) + AP＋cutin 持ちを置き
  候補集合を assert。
- 「AP＋」包含判定は ability.description が正 (エラッタ後テキスト rules/28)。
- action:declare 発動 (ガード判定前、rules/22 と一致) → 同アクションのコンタクトで手札に加えた
  cutin を使用可 (qAndA 明記) — engine 既定で成立するはずだが出荷時 probe/playwright で実踏する。
- filter.custom escape hatch は不採用 (BUG-159 前例 + family 4枚共有のため宣言 filter が正)。

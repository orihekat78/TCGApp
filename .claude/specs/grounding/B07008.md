# B07008 小嶋元太 grounding (2026-07-10, fable)
> 状態: DEFER (SMALL_GAP 1点 — in-hand level の per-count union delta)

## engine gap
- DEFER 行は **半分 stale**: 「in-hand level modifier 無」は解消済 — mini-wave #4 (2026-07-10) で
  `effectiveHandLevel` (`src/engine/flow/main/hand-use-card.ts:105-120`) +
  continuousModifier `lvlOverrideInHand` / `lvlDeltaInHand` (`src/engine/types/card-def.ts:198-208`)
  が出荷済 (consumer B01009/B09095、手札の使用/ネクストヒント/UI 全 4 site)。
- **残 gap = per-count 化**: lvlDeltaInHand は静的 number のみ。B07008 は
  「[阿笠博士] か [少年探偵団] のキャラ **1枚につき** レベル-1」= 自現場の
  union count (cardName OR trait = filterAny) × (-1)。
- **fix**: continuousModifier に `lvlDeltaInHandPer?: { delta: number; filterAny: TargetFilter[] }`
  を追加し、effectiveHandLevel の walk 内で
  `candidates(state, {area:'scene', side:p, filterAny})` 計数 × delta を加算 (~8 行 additive、
  filterAny の OR 意味論は candidates.matchesFilters 既存)。既存 2 field 未使用カードは no-op。
  ※ 汎用 dyn 化 ($self.sceneMatchCount 系) より field 追加が honor site 1 箇所で閉じて安全
  (continuousDeltaSafe 再帰系を踏まない — 計数は matchOneFilter の静的評価のみ)。

## DSL 案
- a1 (continuous, condition: `and[{partnerColor:'青'}, {caseStatus:'解決編'}, {turn:'self'}]`):
  `continuousModifier:{ lvlDeltaInHandPer:{ delta:-1,
    filterAny:[{cardName:'阿笠博士', kind:'character'}, {trait:'少年探偵団', kind:'character'}] } }`
  ← 要 gap fix。ability.condition は effectiveHandLevel が evalCond で honor 済 (:113)。
- a2 (triggered enter selfOnly, condition:{fileAtLeast:5}):
  optional{ chooser:'self', then: sequence[
    `sceneSetState{uid:'$self', state:'sleep'}` (「このキャラをスリープさせてもよい。そうした場合」),
    `sceneToHand{player:'opp', max:1, filter:{levelMax:8, kind:'character'}}` ] }
  — 【FILE5】はネクストヒント直後の resolve 時に再評価され FILE4 なら不成立
  (Q&A 一致 — condition は解決時参照 rules/25)。

## tier + 理由
- **SMALL_GAP** — additive 1 点 (card-def field + effectiveHandLevel 1 loop、honor site 単一)。
  a2 は既存 primitive のみ (fileAtLeast / optional / sceneSetState $self / sceneToHand 短縮形)。

## 罠
- per-count は **hand gate 専用** (Q&A: 現場では元レベル8) — scene 側 level 読みに漏らさない
  (effectiveHandLevel 内で閉じる。candidates levelMax 等は不変)。
- ネクストヒント Q&A「元のレベルを参照」(B01005 との複合) — nhLvl は effectiveHandLevel 適用済
  だが、**他カードが手札のこのカードのレベルを参照する場合**は元レベル (本 modifier は
  手札使用 gate の 4 site のみ)。この非対称は公式 Q&A 通り。
- union 二重計上なし: filterAny は per-char OR 判定 → 阿笠博士かつ少年探偵団特徴でも 1 枚は 1。
- レベル負値も FILE 比較にそのまま (下限なし rules/19、:120 コメント済)。

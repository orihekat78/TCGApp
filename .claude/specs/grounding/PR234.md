# PR234 毛利蘭 grounding (2026-07-10, fable)
> 状態: DEFER (SMALL_GAP, T2)

## engine gap (DEFER 行 3 点の再検証 — 1 点 stale)

1. **charSetCard cardIds branch の faceUp ハードコード — 現存** (src/engine/effect/atom-handlers/char.ts:299
   `mutate.char.setCard(s, scUid, cid, false)`)。line2 は「裏向きで」と印字されず = 表向きセット
   (rules/16 既定。line3「表向きでセットされていた」/ B01023 fromSelf 自己セットも faceUp=true と整合)。
   → additive: cardIds branch で `a.faceUp !== false` を honor (既存 consumer B08036 は裏向き = 引数明示で回帰0)。
2. **hand∪remove union source 不在 — 現存**。pick 候補は query.area 単一 (src/engine/target/candidates.ts:232
   switch(area) / atom-pick-spec.ts:106 buildShortFormPick も単一 area)。「手札かリムーブエリアにある」を
   1 pick で出せない。choice 2 分岐での代替は 1対1 でない (両 zone 横断の単一選択が印字)。
   → additive: query.area の配列受理 (candidates ループ) or `areas:[...]`。
3. **setcard:leave の faceUp filter 不在 — ✅ STALE**。`setCardMatches` (src/engine/cond/eval.ts:630) が
   payload {setCardId, faceUp} を評価し **faceUp!==true は必ず false**。setcard:leave payload は同形
   (mutate/char.ts:380 / mutate/scene.ts:187 emitSetCardLeaves 両経路とも faceUp を運ぶ) → matcherCondition
   でそのまま使える。B07034 の known-gap FIX-3 は解消済。
4. (minor) line3「その中から1枚」の厳密対象 = `$trigger.setCardId` だが、atomHandAddFromRemove の
   target は normalizeTargetToString のみで resolveBindRef を通らない (src/engine/effect/atom-handlers/core.ts:984)。
   filter:{cardName} 代替は同名別 printing (B01023/B01023P) が remove に混在すると観測差あり。
   → additive 1行: target に resolveBindRef を通す。

## DSL 案 (全句)

- 突撃: `keywords:['突撃']`。
- a1【登場時】: trigger{hook:'enter', selfOnly} → sequence[
    bindPick{player:'self', side:'self', n:1, bind:'$host'} (自分の現場キャラ1枚 — B08035 idiom),
    charSetCard{uid:'$host.uid', cardIds:'$pick.cardIds', faceUp:true ←gap1,
      target:{kind:'pick', query:{area:hand∪remove ←gap2, side:'self',
        filter:{cardName:'シャッフルロマンス', kind:'event'}}, n:{min:0,max:1}}} (B08036 同型 source-splice)]。
  0枚 pick は chainStepNoApply (char.ts:267)。host 先 pick は印字と順序逆だが結果等価 (罠参照)。
- a2【相手ターン中】【ターン1】: trigger{hook:'setcard:leave',
    matcherCondition: and[{kind:'triggerPlayerIs', side:'self'} (自分の現場キャラ=B07034 同型),
                          {kind:'setCardMatches', filter:{cardName:'シャッフルロマンス'}} (表向き gate 内蔵)]},
    condition:{kind:'turn', player:'opp'}, limit:{kind:'turn', n:1},
    effect: optional{ handAddFromRemove{player:'self', target:'$trigger.setCardId' ←gap4} } (「〜てもよい」rules/15)。
  Q&A「効果リムーブ / host 離場の両方で発動」= removeOneSetCard emit (mutate/char.ts:379) +
  emitSetCardLeaves (mutate/scene.ts:301,406,445,482,565 全離場経路) で両立済。

## tier + 理由

**SMALL_GAP (T2)** — additive 3 点 (faceUp honor 1行 / area union / resolveBindRef 1行)、いずれも既存
branch 内の局所変更で骨格新機構なし。WRITE 側 (charSetCard) を触るため T2 (2 lens)。

## clone 元 (shipped)

- setcard:leave observer: **B07034 小泉紅子** (triggerPlayerIs side:self) / B02020 (side:opp) / PR231。
- charSetCard cardIds source-pick: **B08036** (remove→set、source-splice 契約)。
- セット対象 (シャッフルロマンス 本体): **B01023** 出荷済 (fromSelf faceUp 自己セット) — line3 の
  「表向きでセットされていた」前提と実機整合。

## 罠

- 手札からセットしても B01023 a1 は発動しない (hook effect:declared kind:'event-use' のみ) — Q&A と一致、追加 gate 不要。
- host 2枚セット同時離場 → emit 2回 だが【ターン1】で1回のみ。B07034 Q&A (per-emit) と同型。
- bindPick 先行のため「イベント0枚にしたいのに host を先に選ばされる」UX — 結果等価 (0枚→セット不成立) だが
  playwright で human 経路確認 (carrier-reuse human 経路実測の教訓)。
- PR240 は同一印字 (parallel) — 本判定をそのまま適用、DSL は spread。

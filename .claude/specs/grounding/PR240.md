# PR240 毛利蘭 grounding (2026-07-10, fable)
> 状態: DEFER (SMALL_GAP, T2) — **PR234 と同一印字 (parallel art、cardNo 0932 共通)**

## 判定

**SMALL_GAP (T2)** — 全面的に [PR234.md](PR234.md) を参照。gap / DSL 案 / 罠 すべて同一:

1. charSetCard cardIds branch faceUp ハードコード (src/engine/effect/atom-handlers/char.ts:299)
2. hand∪remove union pick source 不在 (src/engine/target/candidates.ts:232)
3. ~~setcard:leave faceUp filter~~ = STALE (setCardMatches eval.ts:630 で解消済)
4. (minor) handAddFromRemove target の resolveBindRef 不通 (core.ts:984)

## DSL 案

PR234 の CardDef を authored 後、`{ ...PR234, id:'PR240', imageUrl:'1769159371845748.jpg' }` の
spread で出荷 (B02020P / B07034P と同じ parallel 運用)。abilities は完全共有。

## 出荷順

PR234 と同一 commit で一括 (parallel は検証スキップ規約 — P spread 扱い)。

# B02039 工藤優作 grounding (2026-07-10, fable)
> 状態: DEFER (SMALL_GAP, T3)

## engine gap (DEFER 行「set-card を表向き証拠として持ち主が得る verb 不在」— 現存 + 補足 1 点)

1. **set-card→証拠 転送 verb 不在 — 現存**。現行の set-card 離脱経路は removeOneSetCard
   (src/engine/mutate/char.ts:353-384) の remove 行き 1 本のみ。証拠 push 自体は
   mutate.evidence 側に faceUp 引数付きで存在 (src/engine/mutate/evidence.ts:27,128 —
   `evidence.push({cardId, faceUp, origin})`) → 転送 verb は両既存 primitive の橋渡しで小粒。
   → additive: 新 atom `charSetCardToEvidence` — host の setCards から 1 entry を splice し
   **host 所有側** の evidence へ faceUp:true で push (「持ち主はそのカードを表向きで証拠として得る」。
   engine は setCards に owner を持たない = host owner が持ち主、離場時 remove 行き先と同一規約
   scene.ts:295 と整合)。setcard:leave は emit しない (リムーブエリアに置かれるのではない —
   PR234 系 observer の誤発火防止。emit 要否は出荷時に rules/16 再確認)。0 entry / skip 時は
   chainStepNoApply を立て「そうした場合」後続を gate (charSetCard char.ts:267 と同流儀)。
2. **set-card entry 単位の pick 不在 — 現存 (補足)**。Q&A「裏向きでセットされているカードを選べる」
   = host に複数 set card がある場合 **どの 1 枚か** を選ぶ必要があるが、pick 候補 enumeration
   (src/engine/target/candidates.ts:232 switch(area)) に setCards entry を出す source が無い。
   charRemoveSetCard は末尾 pop / faceDownOnly のみ (atom-handlers/scene.ts:344-368) で選択不可。
   → additive: 2 段 pick (host char pick filter:{hasSetCards} → entry index pick) か、
   candidates に area:'set-cards' 相当の enumeration を追加。裏向き entry は表面情報を見せない
   modal 表示 (裏面 + index) が要る — **UI 配線が絡むため T3**。

## DSL 案 (全句)

- a1: `{ type:'declared', condition:{kind:'bond', name:'黒羽盗一'} (【絆】— パートナー不可は bond 実装済),
    limit:{kind:'turn', n:1}, cost:{kind:'sleepSelf'},
    effect: chain[
      charSetCardToEvidence{host: pick{area:'scene', side:'either', filter:{hasSetCards:true}, n:1},
        entry: pick(setCards 全 entry、裏向き可) } ←gap1+2,
      sceneRemove{player:'self', max:1, side:'either'} (「キャラを1枚まで選び、リムーブ」0枚可 rules/15、
        Q&A「リムーブするキャラは自分が選び、どちらの現場でも」= chooser self / side either) ] }`
  「そうした場合」= chainStepNoApply gate (得なかった場合 sceneRemove 不実行)。
  Q&A「相手のカードも選べる → 相手が表向きで証拠として得る」= host side:'either' + 証拠行き先は
  host owner 側 (verb 内で解決、args.player 固定にしない)。
- a2: `{ type:'triggered', trigger:{hook:'enter', selfOnly},
    condition:{kind:'partnerColor', color:'白'},
    effect: sequence[ atom draw{player:'self', n:2},
      atom discard{player:'self', n:2} (「手札を2枚リムーブする」必須 — n:2 min 固定、多数 shipped 同型) ] }`

## tier + 理由

**SMALL_GAP (T3)** — gap は additive 2 点 (転送 verb + entry pick source) だが、entry pick は
candidates + EffectPickerModal/CardListModal の UI 配線を伴う (「N枚 pick は playwright 必須」
wave-10 教訓)。骨格の既存経路変更は無し。playwright で decoy (set card 2枚 host / 裏向き混在 /
相手現場 host) を置いた実機検証を必須とする。

## clone 元 (shipped)

- 証拠 faceUp 転送の類型: handToEvidence (hand→裏向き証拠) — 逆パラメタ化の参考。
- hasSetCards filter pick: charRemoveSetCard 系 (B02033/B08035) — host pick 部の同型。
- chain gate: B03053 (加えた場合〜) / charSetCard 0-pick gate (char.ts:267)。

## 罠

- 「持ち主」= host owner を規約とする (engine が setCards owner を保持しない現状の唯一解釈。
  相手効果が自キャラにセットする card が将来出たら要再裁定 — 推測補完しないこと)。
- 裏向き entry を選ぶ UI は表面を見せない (rules/16「裏向きセットの表面は確認できない」)。
  得るときに表向き化 (Q&A「表向きにして証拠として得る」)。
- リフレッシュ相互作用: 証拠へ行くため remove を経由しない — deck-out/refresh 計上に影響なし。

# PR022 江戸川コナン

## Source

- Official local snapshot: `.claude/specs/cards-data/pr-01/partner.tsv` (`PR022`, `P001/PR022`).
- Dossier: `.tmp/_ground/PR022.md` generated 2026-08-13; registered, no deferred printed clause.

## Printed text

- 【事件解決】: standard partner solve-case action.
- 【アシスト】: moves the sleeping partner to FILE; resolves the case at **FILE 8+**.

## Implementation

- `standardPartnerActions: true` owns both built-in partner actions; `abilities` remains empty.
- `partnerAssistFileThreshold: 8` is the only non-default partner threshold.
- Engine, AI, UI confirmation, tutorial copy, compiler, and generator read or preserve the shared threshold contract.
- The generated partner barrel is the sole runtime owner of the marker and FILE-8 assist override.
- Ordinary FILE growth and its progress meter remain fixed at FILE 7; solve-case evidence requirements remain unchanged.

## Negative equivalence

- Do not apply FILE 8 globally; all other partners default to FILE 7.
- Do not branch on `PR022` in consumers or encode the built-in actions as card DSL abilities.

## Evidence

- `tests/engine/mutate/partner.test.ts`
- `tests/ai/policies/heuristic.test.ts`
- `tests/ui/hooks/useActionsPanelFlow.assist.test.ts`
- `tests/scripts/gen-partners.test.ts`

# B05063/B05063P 園子のアブない夏物語 (事件) grounding (2026-07-10, fable)
> 状態: DEFER (SMALL_GAP 1点 — toHandOnTurnEnd turn-end branch)

## engine gap
- **唯一の gap**: rider「ターン終了時、このキャラを現場から手札に移す。」の consume。
  `src/engine/flow/turn.ts:83-92` の turn-end consume は `removeOnTurnEnd` / `toDeckBottomOnTurnEnd`
  の 2 flag のみ。`toHandOnTurnEnd` branch 不在。**mutate.scene.toHand は既存**
  (`src/engine/mutate/scene.ts:431`) — filter に flag 1 本追加 + `sceneMutator.toHand(state, uid)`
  呼出の ~4 行 additive で閉じる。
- DEFER 行の「grant 非キーワード ability」は stale — charSetTurnEffect flag 方式 (B07079 a2 出荷済
  idiom) で足り、charGrantAbility 不要。事件カード【宣言】cost flip も stale — `flipFaceUpEvidence`
  cost 出荷済 (D08026/D11021 consumer、canPay = 裏向き≥n.min `src/engine/cost/evaluate.ts:161-164`
  → Q&A「2つ以下では使用不可」と一致。exact-3 は n:{min:3,max:3})。

## DSL 案
- a1「解決編になったとき手札1枚リムーブ」= `_shared/caseResolvedHandRemove({n:1})` **そのまま**
  (case:to-resolved hook + scope:'always'、D06019/D09027 同型)。
- a2 (declared, scope:'always', limit turn1):
  - condition: `and[{caseStatus:'解決編'}, {sceneHas, query:{area:'scene',side:'self',
    filter:{trait:'鈴木財閥',kind:'character'}}, nMin:3}]`
  - cost: `{kind:'flipFaceUpEvidence', n:{min:3,max:3}}` (自分の証拠のみ — cost は自カード限定
    rules/21、Q&A 一致)
  - effect: sequence[
    `sceneEnter{player:'self', from:'hand', max:1, viaEffect:true,
      filter:{cardName:'京極真', levelMax:8, kind:'character'}, bind:'$entered'}`
    (bind = scene.ts:238-240 単一 path 出荷済),
    `charSetTurnEffect{uid:'$entered.uid', key:'toHandOnTurnEnd', val:true}` (B07079 a2 同型) ]

## tier + 理由
- **SMALL_GAP** — additive 1 点 (turn.ts filter+分岐 ~4 行、既存 2 flag と並列。既存カード未使用
  flag = 回帰0)。カード側は全句 clone (caseResolvedHandRemove + D08026 cost + B07079 rider)。

## 罠
- turn.ts consume は phase:end:start trigger 後・clearTurnEffects 前に挿入済み位置 — 同 loop に
  追加すれば「ターン終了時まで」失効前に発火が保証される。
- Q&A: rider は変装先に引継ぎ (rules/23 turnEffects 引継ぎ) — engine 側 disguise が turnEffects
  維持済み (B07079 同 text 出荷済)。probe で 1 本踏むこと。
- 登場は色制限対象 (手札の使用でなく効果? — これは【宣言】効果による登場 = rules/20 色制限外)。
- 手札に移す = リムーブでない → 【現場リムーブ時】不発動 (toHand が正しく非 emit であること確認)。

# B03094 / B03094P grounding

- Authority: fresh isolated 2026-08-25 fetch; `ct-p03/character.tsv` SHA-256
  `67b6c7e245786a7fbc9030e4bfe43a5d594b522f9f71bd756b34f068c230f609`.
- Both printings have identical printed abilities and Q&A text.
- Partner-yellow Q&A: only the continuous Assault ability is conditional; the
  `action:declare` ability has no partner condition.
- Timing Q&A: action declaration fixes the target and sleeps the actor before
  the optional mill resolves; guard remains blocked until that resolution.
- Exact-two Q&A: `mill n:2 gate:true` leaves a one-card deck untouched and the
  chained AP increase does not resolve.
- Repeat Q&A: no Turn1 limit exists. Each new action may resolve the optional,
  while `charModifyAP scope:'action'` is cleared at every action end.
- Verification: public base/P and both-owner action, guard, exact-one deck,
  reactivation, repeat-action, and expiry matrices in Wave149.

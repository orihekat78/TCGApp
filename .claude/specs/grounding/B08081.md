# B08081 広田雅美 grounding (2026-07-10, sonnet5)
> 状態: DEFER (T3)

## engine gap
- a2「【相手ターン中】【ターン1】自分の現場にいるこのキャラ以外のキャラが相手の能力や効果に
  よって選ばれたとき、相手は手札を1枚リムーブしてもよい。そうしなかった場合、それを無効に
  する。この能力は自分の現場に【黒】以外の色を持つキャラがいる場合に発動する。」= B02067/
  B04003 と同一クラスタ (⑦) の pick-finalization intercept 不在。詳細は
  [B02067.md](B02067.md) 参照。「【黒】以外の色を持つキャラがいる場合に発動」の常時条件は
  既存 `sceneHas{query:{filter:{colorNot:'黒'}}}` 相当で表現可 (colorNot は TargetFilter に
  既存)。excludeSelf の「このキャラ以外」も既存 field で表現可。
- a1「【解決編】【登場時】手札を1枚リムーブしてもよい。そうした場合、相手の現場にいるレベル8
  以下のキャラを1枚まで選び、手札に移す。」= 単独なら表現可 (D11007/B01028 系の chain +
  「してもよい。そうした場合」パターン、既存 exemplar 多数)。
  `trigger:{hook:'enter', selfOnly:true}` + `condition:{kind:'caseStatus', v:'解決編'}` +
  `chain[discard{max:1}, sceneToHand{filter:{levelMax:8}, target:opp, n:{max:1}}]`。

## DSL 案
- a1 のみ (a2 は B02067 と同じく不可):
  `chain` steps: `[{kind:'atom', verb:'discard', args:{player:'self', max:1}},
  {kind:'atom', verb:'sceneToHand', args:{target:{kind:'pick', query:{area:'scene', side:'opp',
  filter:{levelMax:8}}, n:{min:0,max:1}}}}]`

## tier + 理由
- T3 (a2 が B02067 と同型の resolver 内部拡張を要求するため)

## 罠
- a1 だけなら T1/T2 相当で出荷できそうに見えるが、a2 が主要な妨害テキストであり
  部分出荷は不可。B02067 の gap 解消と同時出荷が前提。

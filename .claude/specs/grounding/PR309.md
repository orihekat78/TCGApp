# PR309 grounding

- Official raw: `1162/PR309` 降谷零。黄パートナーで突撃[キャラ]。相手ターンに自身がリムーブならデッキ上1枚を非公開で見て任意リムーブ/戻す。解決済み事件アクションが証拠をリムーブしたら実行キャラをスタン。
- Reuse: `partnerColorKeyword`, private `deckRevealUntil`, `boundToRemove`, and B08006 `$trigger.byUid` stun actor pattern.
- QA/equivalence: private reveal never publishes to opponent; action actor, not a selected target, is stunned.
- Negative: declined top card stays deck; trigger is self-only/opponent turn; non-action evidence removal does not stun.

# B03041 grounding

Source: pinned official `ct-p03/event.tsv` (`a1c04fd314135f1af6f71a4fdcc3778ba771810269c4f09fb9fc0429546eb8c7`).

## Printed behavior

- Set this event on one own green scene character.
- The host forces an available guard when it acts and gets AP +2000 during each contact it participates in.
- Official QA says the AP ability resolves when contact occurs, before cut-in actions, including when the opponent targeted the host or the host guarded.

## Adjudication

- The contact matcher now binds the exact set host to either `aUid` or `bUid`.
- Wave 32 proves the AP modifier is active in `action-1`, before the cut-in window advances, on an opponent-caused contact.
- Mandatory-guard behavior is unchanged and outside this Wave's newly adjudicated records.

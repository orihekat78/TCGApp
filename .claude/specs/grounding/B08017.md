# B08017 / B08017P grounding

- Printed: in blue-and-black case, enter up to one level-7-or-less `江戸川コナン` from remove and set this event on it; while the host sleeps, friendly `灰原哀` cannot be selected by opponent abilities/effects.
- Mapping: event-use trigger, remove `sceneEnter`, face-up `charSetCard`, `on-set-host` continuous `untargetableByOppEffectAura` conditioned on host sleep.
- Q&A: action targets remain legal; cut-in and hirameki effects are protected; stun/face-down/host absence disables the rider.


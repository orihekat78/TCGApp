# B07100 コルン grounding (2026-07-10, fable)
> 状態: DEFER (SMALL_GAP)

## engine gap

DEFERRED-INDEX の blocker 記述 (612行「opponentHand/handReveal verb ゼロ」) は **stale**:
- 相手手札の全公開 = 出荷 idiom あり (log atom、D05004/B01074/**B06084** — B06084 は公開→pick 連結の同構造)
- 相手手札からの filtered removal = `discard{player:'opp', filter}` で cross-side 列挙/実行とも動く
  (candidates.ts case 'hand' は side 相対解決、B04058「相手は手札を1枚リムーブ」出荷済 + BUG-175 修正で
  pick 解決後座標系も correct)
- handAtMost{player,n} 条件 = 出荷済 (cond/eval.ts:203)

**残 gap 1点 = pick chooser 帰属**。印字「その中から…1枚まで選び、**相手はそれを**リムーブする」—
選ぶ主語は無標 (= 能力所有者/自分)、リムーブ実行者のみ「相手は」と明示 → **自分が選ぶ**。
現行 atomDiscard (src/engine/effect/atom-handlers/core.ts:51) は byPlayer=dcP (手札所有者=opp) 固定
(buildShortFormPick chooser / tryRePickFromAtom byPlayer とも) → pick が opp 側に surface する。
BUG-175 RCA も「『相手が選ぶ』方向は owner-pick 化する dead 配線」と記録 — 逆方向 (self が opp 手札から
選ぶ) の byPlayer override が不在。

必要 primitive (小粒 additive 1点): atomDiscard に `chooser:'source'` 様の arg を追加し
byPlayer=ctx.source.player を tryRePickFromAtom / buildShortFormPick に渡す (core.ts:51-75 の 2 site)。
pendingEffectPick.ownerPlayer 分離 (BUG-175、pending-state.ts:77 / resolve-picks.ts:417) が既に
chooser≠owner の再実行座標系を支えるため、下流 (apply-pick / UI CardListModal / chooseAiPick) は既存で動く見込み。

## DSL 案 (a1 全文 = 1 ability)

```
{ type:'triggered', scope:'on-scene',
  condition:{kind:'partnerColor', color:'黒'},
  trigger:{hook:'enter', selfOnly:true},
  effect:{kind:'sequence', steps:[
    {kind:'atom', verb:'log', args:{player:'opp', action:'reveal-hand',
      result:'相手は手札を公開する（その後、手札を元に戻す）'}},          // D05004/B06084 idiom
    {kind:'chain', steps:[
      {kind:'atom', verb:'discard',
        args:{player:'opp', max:1, chooser:'source',                       // ★gap arg
          filter:{keyword:'カットイン', levelMax:8}}},                     // BUG-122 defHasKeyword + def-level
      {kind:'conditional',
        if:{kind:'handAtMost', player:'opp', n:4},
        then:{kind:'atom', verb:'draw', args:{player:'opp', n:1}}} ]} ]} }
```
- 「カードをリムーブし、〜場合」= chain gate: 0枚 pick/decline は chainStepNoApply で後段 skip
  (resolve-picks.ts:544/588、B09061 同機構)。handAtMost はリムーブ適用後の手札を読む (rules/25 前段適用後判定)。
- 「レベル8以下のカード」= kind filter なし + levelMax:8 (candidates.ts 数値 filter は card candidate で def 直読)。

## tier + 理由

**SMALL_GAP (T2)** — additive 1点 (chooser 帰属 arg)。emit/WRITE 新設なし、BUG-175 infra 流用。
cross-side pick の human/AI 両経路は playwright + probe で実踏必須 (下記罠)。

## 罠

- **rules/17 Q&A (本カード名指し裁定 2件)**: ①条件アイコンで無効な cutin も「持つ」→ defHasKeyword は
  印字静的判定なので既定で正。②**付与された cutin も「持つ」** → defHasKeyword は CardDef 静的のみ。
  ただし手札カードへ cutin を付与する engine repr が現存しない (付与源 B07003 未登録、traitNameGrant は
  board-uid 限定) → 今日は可観測乖離ゼロ。**B07003 出荷時に state-aware presence reader へ拡張必須**
  (DEFERRED-INDEX 既存行と整合)。
- chooser=self が human の場合 EffectPickerModal/CardListModal に**相手手札の cardId** が並ぶ —
  非公開情報の公開が reveal 句と一体で正当だが、UI 実機で「公開→選択」文言一致を playwright 検証。
- 逆に self=CPU の場合 chooseAiPick が opp(human) 手札から選ぶ — AI heuristic の cross-side 適用を probe。
- 「1枚まで」= 0枚 decline 可 (rules/15)。decline 時は後段 draw も skip (chain gate) — リムーブしていないので正。
- discardToRemove の byPlayer (帰属) は ctx.source.player 渡し済 (W3 r17) — 「相手の効果によって」観測者と干渉しない。

# B10003 工藤新一 grounding

- Source: `ct-p10/character.tsv` rows B10003/B10003P; rechecked 2026-07-26 with `npm run ground -- B10003`.
- P: same behavior and text. Only ID, number suffix, rarity, image differ.
- Printed abilities map to: partner-blue `突撃`; on-scene `サッカー選手` trait grant; partner-blue enter `charSetCard` from self deck top face-down; turn-1 action-end choice.
- Choice 1: `moveSetCard { hostUid:'$self', face:'down', destination:{area:'hand'} }`.
- Choice 2: `bindPick` up to one self scene character with trait `サッカー選手` and `hasFaceDownSetCards:false`, then `moveSetCard` to `$host.uid`. Zero target is permitted by 「1枚まで」.
- Q&A: the trait is only active while this character is on scene; it is not inherited by disguise. The action-end ability does not trigger when this source has left scene, but does when another involved character left and the source remains.
- Q&A: nobody, including the owner, may inspect a face-down set card. The move primitive therefore exposes only occurrence ID/ordinal while choosing.
- Capability: `moveSetCard` preserves the selected physical occurrence and face state, validates source/destination at resolution, and fails closed on stale hosts.
- Tests: `tests/cards/ct-p10/set-move-cards.test.ts`; engine/UI primitive coverage: `tests/engine/effect/move-set-card.test.ts`, `tests/ui/components/DecisionModalHosts.test.tsx`.

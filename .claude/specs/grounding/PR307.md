# PR307 grounding

- Official raw: `1160/PR307` 灰原哀。自ターンAP+1000。青黒事件/青パートナーで登場時、手札1枚リムーブ、レベル7以上を休眠して1枚引き、このキャラで相手キャラへコンタクト。相手ターンにリムーブならデッキ上1枚を任意でリムーブ。
- Reuse: existing `removeFromHand`, `sceneSetState`, `draw`, `bindPick`, `startContact`, `mill` primitives.
- QA/equivalence: as in `B09105` and `PR138`, the final `してもよい` governs the whole coordinated discard-and-sleep action. After accepting, each step resolves as far as possible in order; only a successful sleep reaches draw/contact. The contact actor is the source card.
- Negative: declining changes nothing; accepting with no hand stops immediately; accepting with a hand but no active own L7+ removes the selected hand card, then stops. The leave trigger is self-only and opponent-turn only.

# B10026 稲尾一久

- 根拠: CT-P10 `character.tsv`、公式Q&A（裏向きセットは内容を確認不可）。
- 【宣言】【スリープ】は `declared` + `sleepSelf`。
- 「自分か相手の現場」は `side: 'either'`。裏向き限定は `hasFaceDownSetCards` / `faceDownOnly`。
- 「してもよい」は短縮pickの `max: 1`。0枚または候補なしでは `$removedSet` はmatchedにならず後段なし。
- 除去済みセットカードをbindし、`levelMaxBound` でその印字レベル以下のキャラを選ぶ。
- Q&A・カード本文にない追加挙動なし。

## Wave62 public privacy proof

- Pinned CT-P10 SHA-256:
  `c8592b91ab6281829f40ff2018d32c0493f62ca424483f60edaf5ced886a6d8d`.
- B10026/P public picks mark own/either-side face-down occurrences hidden and
  omit their identities. Owner=`opp` has the same boundary.
- After removal, the now-public level-4 card permits the level-4 target but not
  the level-5 decoy, proving the value is read only after reveal.

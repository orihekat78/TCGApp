# B06003/B06003P 毛利蘭＆江戸川コナン (MR キャラ) grounding (2026-07-10, fable)
> 状態: DEFER (SMALL_GAP 1点 — self LP-delta turn-scope cost)

## engine gap
- **唯一の gap** (DEFER 行は正): cost〚ターン終了時までLP－2する〛= 自身への turn-scope LP デルタを
  コストとして支払う primitive が Cost union に無い (`src/engine/types/effect.ts:522` 周辺の
  21 kinds に該当なし)。
- **fix**: 新 Cost kind `selfLpDeltaTurn { delta: number }` —
  canPay = 常に true (LP は下限なし rules/19、公式Q&A「LP1以下でも支払可・負値可」) /
  pay = 既存 charModifyLP 相当の lpMod_turn 書込 (mutate.char)。
  5-site additive: union (types/effect.ts) + evaluate.ts canPay + pay.ts + UI cost 表示 + validate
  (memory reference-additive-engine-honor-sites「新 Cost」パターン、1 primitive)。
- 他句は全部既存。特に **a3 の宣言条件は engine コメントが本カードを名指しで設計済**:
  `src/engine/types/effect.ts:33-34` 「自分の現場にキャラが3枚以上いて、全員のLPの合計が2以下」
  = `and[sceneHas{nMin:3}, sceneLpSum{max:2}]`。sceneLpSum は負 LP も合算 (charRead.lp honor、
  Q&A 一致)。

## DSL 案
- keywords: ['突撃'] (印字キーワード)。
- a1 (declared, limit turn1, condition:{partnerColor:'青'}):
  - cost: `{kind:'selfLpDeltaTurn', delta:-2}` ← 要 gap fix
  - effect: `sceneSetState{player:'self', side:'either', max:1, state:'sleep'}`
    (「キャラを1枚まで選び、スリープさせる」= side 指定なし either、rules/15)
- a2 (declared, limit turn1, **scope:'on-partner-area'** — 「パートナーエリアでも宣言できる」
  triggered.ts:181/200 で PA 所在 declared 対応済):
  - condition: `and[{sceneHas, query:{area:'scene', side:'self', filter:{kind:'character'}}, nMin:3},
    {sceneLpSum, side:'self', max:2}]` (PA 在中の自身は scene 計数に入らない — area:'scene' query が
    そのまま Q&A「現場にいないので含みません」を満たす)
  - effect: sequence[`draw{player:'self', n:1}`,
    conditional{if:{kind:'handAtLeast', n:5}, then:`discard{player:'self', n:1}`}]
    (「5枚以上ある場合」は draw 後判定 — sequence 逐次評価 rules/25 B08048 型 ✓)
- cutin: 【カットイン】AP＋2000 = D01011 同型。MR core 出荷済。

## tier + 理由
- **SMALL_GAP** — additive 1 点 (新 Cost kind、5-site だが全て既存パターンの並列追加)。
  効果側 3 句 + 条件は全て既存 primitive の 1対1 (sceneLpSum は本カードが設計上の主 consumer)。

## 罠
- cost の LP-2 は「ターン終了時まで」— lpMod_turn は clearTurnEffects で失効 ✓。ただし
  **コストで下げた LP は a2 条件 (LP 合計≤2) の評価にも即時反映される** (盤面読みは実効 LP) —
  a1→a2 の順に使う CPU 方策で意図せず条件成立させうる。仕様通り (悪用でなく正)。
- コストで行った LP 減は「効果によって LP を下げたとき」系の他カード条件を満たさない
  (rules/21 コスト非該当) — selfLpDeltaTurn pay で emit を出さないこと。
- PA 在中で a1 は使用不可 (scope 無指定 = on-scene)。a2 のみ on-partner-area。

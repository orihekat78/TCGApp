# B08066 / B08066P grounding

Source: [Waves182-183 authority](waves182-183-authority.md).

## Printed mapping and QA

- The declared cost sleeps B08066 and moves exactly one Nagano character from
  its controller's remove area to that controller's deck bottom.
- An identically eligible opponent occurrence is never a cost candidate;
  rejected payment is atomic for both owner orientations.
- `removeAreaToDeckBottom` uses `remove.removeFromHere`, so the cost emits the
  cause-independent `remove:exit` hook.
- Shipped B05087 and B05088 both trigger, consume their turn-one occurrences,
  and enter the same owner-order group before later resolution.
- The post-cost Assault target remains zero-or-one and either-side.
- Wave182 retires the stale pre-`remove:exit` DEFER record.

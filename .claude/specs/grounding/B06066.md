# B06066/B06066P 怪盗キッド＆白馬探 (MR キャラ) grounding (2026-07-10, fable)
> 状態: GREEN (DEFER blocker stale — sleepChar trait-filter cost で表現可)

## engine gap
- **無し**。DEFER 行「self-or-shared-trait cost 不在」は stale:
  `sleepChar` cost は TargetingRef pick を取る (`src/engine/cost/evaluate.ts:44-53` canPay /
  `src/engine/cost/pay.ts:55-71` pay、n.max cap + active gate 済)。
  「このキャラか、このキャラと同じ特徴を持つキャラ」= 印字特徴が静的 (怪盗|探偵|高校生) なので
  `filter:{trait:['怪盗','探偵','高校生']}` (any-match、candidates.ts:426-431 wants.some) で
  self 含め全対象を被覆 (excludeSelf 無し = self 自身も候補)。B03060 a1 の excludeSelf 抜き clone。
- a2 の条件「スリープ状態かスタン状態のキャラが合わせて3枚以上」= `sceneHas` +
  `query.state:['sleep','stun']` (TargetQuery.state 実装済 candidates.ts:355-356)。
- スタン→アクティブ = 代わりにスリープ (Q&A) は mutate.scene.setState が実装済
  (`src/engine/mutate/scene.ts:513-520`)。

## DSL 案
- a1 (declared, scope:'on-scene', limit turn1):
  - cost: `{kind:'sleepChar', target:{kind:'pick', query:{area:'scene', side:'self',
    filter:{trait:['怪盗','探偵','高校生']}}, n:{min:1,max:1}, chooser:'self'}}` (B03060 a1 同型)
  - effect: `sceneToDeck{player:'opp', max:1, filter:{apMax:8000, kind:'character'}, pos:'bottom'}`
    (短縮形出荷済 scene.ts atomSceneToDeck)
- a2 (triggered, scope:'on-partner-area' — 「パートナーエリアでも発動」triggered.ts:200 で
  scene+PA 両所在 OK):
  - trigger: `{hook:'phase:end:start', ...}` + condition `and[{turn:'self'}, {sceneHas,
    query:{area:'scene', side:'self', kind character, state:['sleep','stun']}, nMin:3}]`
    (自分ターン終了時 gate は triggerPlayerIs/matcher player==='self' でも可)
  - effect: `sceneSetState{player:'self', side:'either', max:1, state:'active'}`
    (「キャラを1枚まで」= side 指定なし → either、rules/15)
- cutin: 【カットイン】AP＋2000 = D01011 同型 (on-hand + effect:declared + $contact.byUid)。
- MR = core 出荷済 (bef3adad+5352c470)、カード側は kind/mr フラグのみ。

## tier + 理由
- **GREEN** — 全句が既存 verb/cond/cost/filter で 1対1。clone 元: B03060 a1 (sleepChar cost) /
  B09108 a2 (PA-declared/triggered 所在)。

## 罠
- sleepChar cost の「どの1枚を sleep するか」は ctx.picked 未配線で head-fixed
  (pay.ts:54 記載の全 target-pick cost 共通制約、B03060 と同 posture) — 新規 regress ではない。
- 効果で特徴を付与されたキャラも「同じ特徴」に該当 (grantTraits honor 済 candidates.ts:429)。
  逆に B06066 自身の特徴が動的に変わるケースは静的 list では追従しない (現 pool に該当效果なし、
  out-of-scope note をカードコメントに残す)。
- a2 は「1枚まで選び」= 0枚可 — optional でなく n:{min:0,max:1} pick。発動自体は必須 (rules/15)。

# B10101 / B10101P grounding

- Source: current-parser CT-P10 case TSV rederived from the retained API snapshot;
  see [authority manifest](waves64-65-authority.md)
  (`1b7529bacd539db35cf92ce5fb4345ad0d549d2694eb188f9288abe2da507de3`).
- Printings: B10101 and B10101P are behavior-identical.
- Resolution transition: when this case becomes resolution chapter, remove one own hand card. General mandatory case-resolution handling applies.
- Declared cost: exactly two own face-down evidence are turned face up. Opponent evidence is ineligible, one evidence is insufficient, and evidence order is preserved.
- Declared target: grant the printed triggered ability to up to one own green or white scene character until turn end. Declining the target is legal.
- Trigger: once per turn, only when an opponent scene character is removed by contact with the granted character.
- Deck window: inspect up to four cards, publicly add up to one qualifying character, then move every remainder to deck bottom in chosen order.
- Qualifier: exact printed `突撃`, or `突撃` made valid by that card's own condition icon. An inactive icon, an ordinary text grant, `突撃[キャラ]`, and `突撃[事件]` are ineligible.
- Short deck: inspect all remaining cards. Refresh happens only after the chosen card leaves the deck or the remaining cards move; declining a qualifying card is legal.
- DSL: `keywordFromPrintOrConditionIcon:'突撃'`, `deckRevealUntil(maxN:4, chooseMatch:'upTo')`, `handAddFromDeck`, then `deckToBottomBound`.
- Evidence: `late-99-101.test.ts`, `b10101-qa.test.ts`, `ctp10-printed-keyword-filter.test.ts`, `rule-manual-ver2.5.test.ts`.
- Q&A trace IDs: shard 6 (5), shard c (2), shard d (1), shard f (1); all require merge-time status update after their focused assertions are complete.

## Wave53 public cost certification

- One own facedown evidence rejects transactionally and consumes no turn use.
- Nonadjacent two-down recovery succeeds in the same turn; opponent evidence
  remains face down. Existing accepted route proves the temporary grant effect.

## Wave57 arbitrary evidence positions

- Base and parallel sources accept `[3,0]` from a mixed evidence row without
  changing evidence identity/order or opponent evidence.
- Malformed selections reject transactionally. The same payment grants the
  printed contact-removal search ability to an eligible green character.

## Wave65 short deck

- Base and parallel cases grant through the real declaration, then real contact
  removal opens a one-card private deck window on the granted character.
- The selected Assault character alone becomes public; hand entry refreshes
  immediately, gives opponent evidence, and leaves no remainder reorder.

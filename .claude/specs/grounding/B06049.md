# B06049 佐々木小次郎 grounding

Source: pinned official CT-P06 character TSV
(`15eab04615778f7ce4c436924e5a8966b91d2f19499cfad9c07de35191ba7bd7`).

## Wave156 completion

- Printed a1 evaluates the other-YAIBA scene condition only when its mandatory
  enter trigger resolves, then grants turn-scope `突撃` to the source.
- A later YAIBA entry cannot retroactively grant it. Once granted, removal of
  the qualifying witness does not revoke it.
- Public hand use plus declared enter/remove effects prove both outcomes for
  both owners, including actual case-action legality.

## Existing suppression lane

- Printed a2 already uses action-declare/case matching and
  `setHiramekiSuppress` on the opponent turn-state slot.
- The Hirameki listener honors the flag and action-end clears it; cluster8
  regressions cover set, scope, suppression, and cleanup. The old DEFER row is
  stale and closed in Wave156.

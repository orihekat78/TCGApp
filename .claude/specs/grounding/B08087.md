# B08087 grounding

Source: current CT-P08 character TSV
(`74f2a658cf64fb970aa22f0db6ef7fa925b08a36acd8162791ed75f802c93545`).

- While B08087 is in scene and is a legal candidate for an opponent-used
  event's own effect, `mustBeSelectedByOppEvent` adds its exact UID to the
  forced selection set.
- Wave185 real event-use owner mirrors reject a decoy-only response without
  state change, then accept B08087 and leave the decoy in scene.
- The constraint is continuous; it creates no independent effect queue.

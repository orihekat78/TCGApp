# B10007 赤木英雄 grounding

- Source: `ct-p10/character.tsv` rows B10007/B10007P; rechecked 2026-07-26 with `npm run ground -- B10007`.
- P: same behavior and text. Only ID, number suffix, rarity, image differ.
- Partner-blue enter ability: `charSetCard` from self deck top onto this character, face-down.
- Declared sleep ability: choose up to one self scene `サッカー選手` with no face-down set card, excluding this source; then move one face-down set occurrence from this source to that host using `moveSetCard`.
- The target is optional (`1枚まで`). `excludeSelf` and the no-face-down filter make the scene destination distinct and legal before opaque set-card choice.
- Second declared ability: pay sleep plus remove one set card from this host, then remove up to one character with AP 6000 or less.
- Q&A: face-down set-card faces cannot be inspected, including by the owner. Choice UI must remain occurrence-only.
- Capability: scene-to-scene movement preserves face state and instance ID, has no leave/enter transition, and rejects vanished or cross-owner destinations before splice.
- Tests: `tests/cards/ct-p10/set-move-cards.test.ts`; engine/UI primitive coverage: `tests/engine/effect/move-set-card.test.ts`, `tests/ui/components/DecisionModalHosts.test.tsx`.

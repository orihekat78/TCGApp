# grounding dossier: B09033

- TSV: デッキ上4枚を公開し、条件一致キャラを1枚ずつ登場させる処理を最大4回繰り返す。残りはデッキ下へ置く。
- 実装: `repeatOptional`。公開windowの同名カードは `cardId#index` で識別する。
- 確認: 人間の実行/停止、`owner=opp`、0選択、重複ID、残カードのdeck bottomをprobeで覆う。

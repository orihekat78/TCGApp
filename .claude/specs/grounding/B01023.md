# B01023 grounding

Source: pinned official `ct-p01/event.tsv` (`833f50e6fa609fc707f23e2aae1130d5a7e4f5d118c0495faaf76688a591660f`).

## Printed behavior

- Look at the top five deck cards, add up to one to hand, return the rest to the deck bottom in any order, then set this event on one own scene character.
- The set host gets AP +2000 during each contact it participates in.
- Official QA permits zero hand additions and use with no host, but requires setting when a host exists; the event is removed with its host.

## Adjudication

- Existing private look, bottom reorder, and mandatory `charSetCard` atoms match the printed sequence and zero-choice branch.
- The contact matcher now binds the exact set host to either contact participant, covering opponent-caused contact without broad observer matching.
- Wave 32 public-dispatch tests directly prove B01023's opponent-caused contact, no-host cleanup, face-up set/removal, and zero choice. The shared D10024 printing proves both contact directions with the same exact-host matcher.

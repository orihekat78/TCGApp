# B10015 毛利小五郎 grounding

## Source

- Primary: `.claude/specs/cards-data/ct-p10/character.tsv`, row `B10015`.
- Only one printing is present: No. 1077, C, blue, Lv.6, AP 5000, LP 1,
  traits `探偵|毛利探偵事務所`, image `1783904094980534.jpg`.
- `B10015P` is absent from the TSV.

## Ability mapping

1. While B10015 is in the owner's scene, `grantTraitsAura` gives only own
   scene characters named `妃英理` the effective trait `毛利探偵事務所`.
2. A continuous `sceneHas` counts four or more distinct names with that
   effective trait and grants B10015 `迅速` while the condition remains true.
3. Optional Hirameki at `evidence:remove-by-action` selects up to one own
   remove-area character named `妃英理` and adds it to hand.

## Q&A decision

- If B10015 has begun an action or reasoning via the conditionally gained
  `迅速`, losing the four-name condition during that action does not end it.
  The reader still removes `迅速` immediately for future permission checks.

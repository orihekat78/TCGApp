# B01006 / B01006P grounding

- Printed: bond `江戸川コナン` grants opponent-effect target protection; declared sleep enters up to one level-5-or-less `少年探偵団` character from hand, then draws only if it is `江戸川コナン`.
- Mapping: `bond` + `untargetableByOppEffect`; `sleepSelf`; hand `sceneEnter` binding; `boundMatchesFilter` draw gate.
- Verified: protected character is excluded only from opponent effect picks, not action target selection; zero enter is legal.


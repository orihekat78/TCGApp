# B10065 / B10065P / B10065P2 grounding

- Source: CT-P10 character TSV and attached official Q&A, rechecked 2026-07-26.
- Printed title: `松田陣平＆萩原研二`; `CardDef.names` records the printed title plus rules/19 components `松田陣平` and `萩原研二`, matching the existing combined-name convention.
- Entry observer: partner yellow, own turn, once per turn; when a named Police Academy peer other than this character enters own scene, optionally remove one Lv9-or-less character.
- Bond trigger: with 降谷零, only at the end of the controller's turn; optionally remove one hand card, then enter one own removed Lv3-or-less Police character asleep. `discard` is the engine atom for hand-to-remove.
- Declaration: once per turn, sleep this card to grant AP+2000 until turn end to one of the printed three names; it is legal from the partner area, so `scope:'on-partner-area'`.
- Printed CutIn is AP+2000 to `$contact.byUid`, encoded as the normal on-hand `effect:declared` optional listener.
- P/P2 printings share behavior. Regression: `review-blocker-cards.test.ts` checks own-turn, PA scope, and CutIn metadata.

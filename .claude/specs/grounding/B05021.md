# B05021 grounding

- Authority: release packet `191a8c7bfae2f027995030322f081837c44d8fcc`, source digest
  `8409a76a2b33dcd65f339b97741ae2e0eaba2fd7175ecd789dc5b9e5e72137c9`,
  `ct-p05/character.tsv` SHA-256 `f9fd90bcd2b0ea41fc7274cde0114f6e4109c92c82f7972ebef7f216e4018074`.
- Printed leave ability: during the opponent turn, when this scene character goes to remove, reveal from
  the top until the first card named `毛利小五郎`, add it to hand, move the other revealed cards to deck bottom,
  then shuffle the deck.
- Official Q&A: the first match is mandatory; no match returns every revealed card before shuffling.
  B05012 is not `毛利小五郎` while in deck and must not match.
- DSL: `leave:to-remove/selfOnly` + opponent-turn condition -> public/all `deckRevealUntil` with exact printed
  name -> conditional exact `handAddFromDeck` -> `deckToBottomBound(order:'preserve')` -> whole `deckShuffle`.
- Fix: preserve revealed order before the mandatory shuffle; do not create an unprinted reorder decision.
- Verification: public positive/no-match, B05012 decoy before a real match, forced acquisition, no reorder,
  timing/selfOnly negatives, and terminal cleanup.


# B02061 grounding

Source: fresh CT-P02 character TSV; see waves78-79-authority.md.

## Wave79 correction and certification

- The shipped descriptor incorrectly used `evidenceGain max:1` in a bare chain.
  The handler reads `n`, so no evidence moved and no optional was presented.
- BUG-344 changes a1 to the proven B01065/B01069 shape: outer optional, then a
  sequence with `evidenceGain n:1` and the up-to-one other-red Assault grant.
- Public self/opp accept/decline, hidden evidence order, target/source exclusion,
  exact-one deck-out, pending save reauthentication, CPU skip, and Hirameki
  fire/skip are card-bound.
- Desktop/mobile browser tests click the optional and direct-scene target UI,
  proving the hidden evidence transfer and decoy exclusion without console errors.

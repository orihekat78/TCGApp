# B10056 勝又水菜

- Source: CT-P10 `character.tsv`, official Q&A column. Grounded by `npm run ground -- B10056` on 2026-07-26.
- Base: character, 赤, Lv4, AP4000, LP1, trait `女流棋士`.
- A1: own turn only; aura gives AP+1000 to each other own scene character having **both** `女流棋士` and `棋士`. `traitAll` is an AND selector; neither trait alone qualifies. The official Q&A confirms it is continuous and ends immediately when invalid.
- A2: Hirameki is the standard optional `evidence:remove-by-action` trigger. Select up to one own-remove character with `女流棋士` **or** `棋士`, then add it to hand. The source card is not yet in remove while this effect resolves, per the official Q&A, so it cannot select itself.
- Rules: `03-field-areas.md`, `10-action-event.md`, `15-abilities-effects.md`, `17-icons.md`.
- Tests: `tests/engine/ctp10-trait-all-filter.test.ts`, `tests/cards/ct-p10/b10056.test.ts`.

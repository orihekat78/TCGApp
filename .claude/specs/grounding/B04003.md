# B04003 工藤新一 grounding (2026-07-10, sonnet5)
> 状態: DEFER (T3)

## engine gap
- a1「【絆毛利蘭】【相手ターン中】【ターン1】自分の現場にいる〚カード名［毛利蘭］〛が相手の
  能力や効果によって選ばれたとき、相手は手札を1枚リムーブしてもよい。そうしなかった場合、
  それを無効にする。」= B02067 と同一クラスタ (⑦) の pick-finalization intercept 不在。
  詳細は [B02067.md](B02067.md) 参照 (resolve-picks.ts に選択確定 hook 0件、consult機構は
  現場離脱専用で流用不可)。bond condition (【絆】) 自体は既存 primitive で表現可
  (`condition:{kind:'bond', name:'毛利蘭'}`)。
- a2「【宣言】【ターン1】【スリープ】〚手札からカード名［毛利蘭］を1枚公開する〛：AP8000以下の
  キャラを1枚まで選び、デッキの下に移す。」= 単独なら表現可 (cost: sleepSelf + revealFromHand、
  effect: atom sceneToDeck{filter:{apMax:8000}, n:{max:1}})。ただしカードとしての出荷は
  a1 の gap に律速される (a1 が未解決のうちは card 全体を出荷不可)。

## DSL 案
- a2 のみ (a1 は B02067 と同じく不可):
  `trigger` なし (宣言能力) / cost: `[{kind:'sleepSelf'},{kind:'revealFromHand', filter:{cardName:'毛利蘭'}, n:1}]`
  / effect: `{kind:'atom', verb:'sceneToDeck', args:{target:{kind:'pick', query:{area:'scene', side:'either', filter:{apMax:8000}}, n:{min:0,max:1}}}}`

## tier + 理由
- T3 (a1 が B02067 と同型の resolver 内部拡張を要求するため)

## 罠
- a2 だけなら T1/T2 相当で出荷できそうに見えるが、a1 の【絆】反応能力が主要テキストであり
  部分出荷は不可 (カード全体の意味論を欠く)。B02067 の gap 解消と同時出荷が前提。

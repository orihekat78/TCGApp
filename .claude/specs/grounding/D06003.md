# D06003 服部平次 grounding (2026-07-10, fable)
> 状態: DEFER (SMALL_GAP)

## engine gap

「【カットイン】AP＋」を持つ = **cutin の効果内容 (テキスト) 判別 filter が宣言形で不在**。
`filter.keyword:'カットイン'` (BUG-122, read/keyword.ts:90 defHasKeyword) は形状判定のみで、
qAndA のウォッカ除外 (B01097 cutin=draw2+discard1、「AP＋」を含まない→選べない) を表現できない。
qAndA の判定基準は **印字テキストが「AP＋」を含むか** (文字列包含、効果構造でない)。

必要 primitive (小粒 additive 1点、4 site 同期):
- `TargetFilter.cutinTextIncludes?: string` — src/engine/types/effect.ts:271 (keyword field 隣) に追加
- 述語 `defHasCutinTextIncludes(def, text)` = `abilities.some(ab => abilityIsCutin(ab) && ab.description.includes(text))`
  — src/engine/read/keyword.ts:24 abilityIsCutin 再利用、同ファイルに追加
- consumer — src/engine/target/candidates.ts:449-454 (filter.keyword ブロック直後、def ベースなので remove-area
  card candidate (c===null) でも動く)
- whitelist — scripts/taskA-validate-specs.cjs:128 FILTER_FIELDS + sync test literal
  (tests/engine/sync-taskA-whitelists.test.ts、TargetFilter 3-way sync の第3点忘れ注意)

代替 (engine 0 で author 可能な escape hatch): `filter.custom` closure (D11005/B09017 出荷精文、
candidates.ts filter.custom honor 確認済)。ただし QA-critical 除外semanticsが関数内に逃げ
machine gate (validate/compiler) 素通り + BUG-159 (custom filter semantic bug) 前例 + 本 filter は
**D06003/D06004/D06021/D06023 の4枚 family 共有** → 宣言 filter 追加が正。

## DSL 案

a1 【事件緑＆白】〚突撃〛 — **GREEN、D06010 a1 の印字同文 clone** (git grep 確認: D06010 は
同 TSV 句「【事件緑＆白】〚突撃〛」):
```
{ type:'continuous', scope:'on-scene',
  condition:{kind:'caseColor', color:['緑','白'], combine:'and'},
  continuousModifier:{ grantKeywords: () => ['突撃'] } }
```

a2 【パートナー緑】【ターン1】このキャラがアクションしたとき、リムーブの「【カットイン】AP＋」持ち1枚まで手札へ:
```
{ type:'triggered', scope:'on-scene',
  condition:{kind:'partnerColor', color:'緑'},
  limit:{kind:'turn', n:1},
  trigger:{hook:'action:declare', selfOnly:true},   // D06010 a2 同型。qAndA「宣言・対象指定・スリープ時点=ガード前」= rules/22 と一致
  effect:{kind:'atom', verb:'handAddFromRemove',
    args:{player:'self', max:1, filter:{cutinTextIncludes:'AP＋'}}} }  // ★gap filter
```
- handAddFromRemove = 出荷済 (B09061 a2 / B03053 a2、ATOM_PICK_SPEC defaultArea:'remove' sourceSplice)
- 「カード」= kind filter なし (character/event 両方の cutin 持ち対象)
- mid-action pick (action:declare trigger + pick effect) は B01036 出荷済で経路実証

## tier + 理由

**SMALL_GAP (T2)** — additive filter 1点 (WRITE 側でなく filter read 側、既存 abilityIsCutin 再利用) で解禁。
解禁で D06004/D06021/D06023 も同時候補 (DEFERRED-INDEX 同行 family)。

## 罠

- ウォッカ除外 probe 必須: remove に B01097 (cutin draw型) + AP＋cutin 持ちを置き候補集合を assert
  (qAndA の名指し decoy)。
- 「AP＋」包含判定は ability.description が正 (エラッタ後テキスト rules/28)。conditional AP+ cutin
  (D11013型「〜の場合 AP＋」) も印字に「AP＋」を含むため一致 — text包含が qAndA の裁定基準と1対1。
- qAndA「同アクションのコンタクトで加えた cutin 使用可」— action:declare 解決 (ガード前) → contact で
  on-hand cutin window。engine 既定で成立するはずだが出荷時 probe/playwright で実踏する。
- charModifyAP 構造 walk 案は不採用: 「AP−」cutin や将来の非 atom 表現で乖離しうる。裁定基準が
  文字列なので文字列で写す。

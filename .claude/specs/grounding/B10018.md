# B10018 / B10018P grounding

- Source: CT-P10 event.tsv; generated dossier `.tmp/_ground/B10018.md`.
- Standard event use: hand event is set face-up on one own `サッカー` character.
- When this belt is set, its host receives the deck top card face-up, then its owner draws one.
- `FILE8` declared use repeats the set action only while no own `ガジェット` card is in the scene.
- Rules/16 privacy: the absence check sees scene characters and face-up set-card entries only. Face-down set cards do not disclose name or trait.
- P is printing-only metadata variation.

Mapping:

- a1: `effect:declared(event-use)` → `charSetCard fromSelf`.
- a2: `on-set-host / setcard:enter` matching B10018 → face-up deck-top set, draw.
- a3: `fileAtLeast(8)` and not `sceneHas(scene ∪ set-card, trait:ガジェット)` → same set action.

# B10102 / B10102P grounding

- Source: CT-P10 case TSV and included official Q&A, checked 2026-07-26.
- Resolution trigger removes one hand card. During 解決編, once per turn, turn two evidence face up and resolve the look-one/add-match/bottom-rest sequence once for each own scene character with no original ability other than CutIn/Hirameki.
- The same official Q&A interpretation as B10074 applies: original printed non-icon text disqualifies even when disabled; granted text does not; CutIn-only and Hirameki-only cards qualify.
- Mapping: `forEach` scene query with `hasNoOriginalAbilityExceptIcons:['カットイン','ヒラメキ']`. P printing shares behavior.
- Regression: `review-blocker-cards.test.ts` asserts the exact shared predicate.

## Wave53 public cost certification

- One own facedown evidence rejects transactionally and consumes no turn use.
- Nonadjacent two-down recovery succeeds in the same turn; opponent evidence
  remains face down. Existing accepted route proves the per-character look.

## Wave57 arbitrary evidence positions

- Base and parallel sources accept non-sorted `[3,0]` while retaining evidence
  identity/order and isolating opponent evidence.
- Five malformed selections reject transactionally. The same payment performs
  one ordered deck look for each of two eligible vanilla scene characters.

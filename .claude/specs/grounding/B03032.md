# B03032 grounding

## QA evidence

Reviewed answer hashes: `e88d...`, `c07c...`, `dc71...`, `7066...`.
All adjudication remains in hash-only shards.

## Current mapping

| Clause | Current state |
|---|---|
| Partner color keyword | `partnerColorKeyword` |
| Entry-time opponent deck-top set card | `charSetCard{player:'opp',fromDeckTop:true,faceUp:false}` |
| Active set-card action-target exception | `expandActionTargets{side:'opp',state:['active'],hasSetCards:true}` (`action:pre-target`, on-scene, selfOnly) |

## Rule roots

- `rules/07-action-flow.md`: normal action targets and target expansion.
- `rules/13-keywords.md`: partner color keyword.
- `rules/16-card-set.md`: set-card host state and leave behavior.

## Required verification

- `B03032` and `B03032P`: only active opponent set-card hosts enter the exception; face-up and face-down set cards qualify.
- Wrong-side and no-set active decoys stay excluded. Sleeping/stunned set-card hosts remain ordinary action candidates, not exception targets.
- The selected exception target cannot guard; another active character can.
- Removing the on-scene source host removes the exception; its stale uid has no candidates.

# B04059 水無怜奈 grounding

## QA evidence

Reviewed answer hashes: `0597...`, `6765...`, `366d...`.
All three now have card-bound coverage.

## Current mapping

| Printed clause | Mapping |
|---|---|
| While in the scene, this character is also card name [本堂瑛海] | `continuousModifier.grantNames` with `scope:'on-scene'` |
| On opponent turn, when removed from scene, sleep a level 5 or lower character | `leave:to-remove` + `turn:'opp'` + `sceneSetState` |

## Rule roots

- `rules/17-icons.md`: scene leave trigger timing.
- `rules/19-special-rules.md`: split/effective card-name handling.
- `rules/24-qa-naming-stun.md`: in-scene additional identity.

## Verification

`tests/cards/official-qa/B04059.test.ts` proves the scene alias through the
reader, bond, and target filter; proves hand/deck/remove exclusion; and keeps
the opponent-turn leave trigger covered.

## Horizontal boundary

`consult-choose-intercept.ts` intentionally reads `targetDef.names` and has no
effective-name reader, so its printed-only target identity excludes this alias.
`distinctNames` similarly uses `allCardNameComponentsForDef`; the regression
proves B04059 and a printed 本堂瑛海 remain distinct for that hidden consumer.

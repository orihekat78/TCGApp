# B08094 / B08094P grounding

Source: current-parser CT-P08 case TSV rederived from the retained API snapshot;
see [authority manifest](waves64-65-authority.md)
(`f152683d703862e846ccd1f5ff58fb1d5f004e8dc2fbdfb703f2c802cf90a778`).

## Wave57 arbitrary evidence positions

- Both physical sources accept `[3,0]` from a mixed evidence layout without
  reordering or changing any evidence identity/origin.
- Invalid counts, duplicates, range errors, and face-up selections reject
  transactionally. The same payment reaches the printed deck look, selects a
  leave-trigger character, and preserves remainder order.

## Wave64 short deck

- Base and parallel cases inspect every card when fewer than three remain.
- The sole selected `現場リムーブ時` character is publicly revealed and causes
  refresh only when it leaves deck; a private unselected remainder does not.

## Wave186 printed ability presence

- The deck search reads printed 【現場リムーブ時】 presence, not whether the
  found ability is currently active.
- An opponent-turn-only holder is legal during the owner's turn. Non-holders
  stay excluded; both owners pass through public dispatch.

# B08019 大岡紅葉＆伊織無我 (MR/MRP) grounding (2026-07-10, sonnet5)
> 状態: DEFER (T2)

## 印字 (要約)
a1【パートナー緑】【自分ターン中】【ターン1】自分の現場にこのキャラ以外の〚カード名[大岡紅葉]〛
か〚[伊織無我]〛が登場したとき、AP8000以下のキャラを1枚まで選びリムーブする。
a2【宣言】【ターン1】自分か相手の現場のキャラに裏向きセットされているカードを合わせて2枚まで
リムーブしてもよい（自分/相手で1枚ずつ）。そうした場合カードを1枚引く。この能力は自分の現場に
〚大岡紅葉〛か〚伊織無我〛がいる場合に宣言でき、**パートナーエリアでも宣言できる**。
+【カットイン】AP+2000。

## engine gap
- **旧 DEFER bullet「scope 単値で on-scene+on-partner-area 併記不可」は stale。**
  `scope:'always'` が両方の gate を通す実装が既に存在（src/engine/flow/main/declared-ability.ts:145-147：
  `ability.scope !== 'on-partner-area' && ability.scope !== 'always'` で PA 拒否 → 'always' は通過）。
  本カードは MR（TSV rarity 列で確認）なので rules/18 の PA 常駐と整合、scope 配列は不要。
- **実在の gap（2026-07-10 付 DEFER note、非 stale）**: `TargetQuery.perSideMax` は engine 側実装済み
  （src/engine/effect/atom-pick-spec.ts:119-120, mega-wave W4）。だが「自分1+相手1、合わせて2枚」の
  **human 側 multi-pick UI 収集**が未対応 — SceneArea は cross-side multi 収集を持たず単発 dispatch に
  collapse する（B02033/B07031/B07055 と同型、BUG-165 cluster）。engine quota ロジックは完成、
  UI 収集層のみ欠落。

## DSL 案
`declared{scope:'always', cost:{removeSetCard, query:{perSideMax:1}, n:2}, effect:draw{n:1}}`。
a1: `trigger{hook:'enter', matcherCondition: triggerCardMatches{name:['大岡紅葉','伊織無我'], excludeSelf:true}}`
→ `pick(scene,side:self,apMax:8000,n:'0-1')` → `sceneRemove`。

## tier + 理由
T2。新規 primitive 不要、SceneArea の multi-pick UI 収集配線のみ（BUG-165 既知クラスタの
延長でバッチ修正可能）。

## 罠
- 旧 DEFER bullet の scope-array 懸念は stale（scope:'always' で解決済み）— 新しい 2026-07-10 note が
  正しく上書きしており、そちらを正とする。
- 単独カードのための engine primitive 追加と誤認しないこと。SceneArea multi-select を直せば
  B02033/B07031/B07055 とまとめて解禁できる横展開機会。

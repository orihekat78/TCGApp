# B09019/B09019P 「くさるなよ！」 (イベント) grounding (2026-07-10, fable)
> 状態: DEFER (SMALL_GAP 1点 — sceneEnter multi 経路の bind 書込)

## engine gap
- DEFER 行「effect 内登場数カウンタ不在」は **半分 stale**: 計数 cond は
  `boundCountCompare{bindKey, cmp, n}` が出荷済 (`src/engine/types/effect.ts:63`、2026-07-10 B08057)。
  残 gap = **sceneEnter の cardIds multi 経路が `a.bind` を ctx.bindings に書かない**
  (`src/engine/effect/atom-handlers/scene.ts:50-106` — enteredGroup は enter:group emit の
  hook-bindings にのみ渡り、単一 path の bind 書込 scene.ts:238-240 に相当する処理が multi に無い)。
- **fix**: multi 経路末尾 (scene.ts:~105、enter:group emit 前後) に
  `if (typeof a.bind === 'string') ctx.bindings[a.bind] = enteredGroup` の ~2 行 additive。
  enteredGroup は **実際に登場した** キャラのみ (scene-full-skip 分を含まない) → 「この効果に
  よってキャラが5枚登場した場合」と 1対1。
- 他は全部既存: fileRemoveTop (アシストパートナー skip 内蔵 core.ts:375-399、Q&A 一致) /
  sceneEnter from remove + cardIds multi + enterSleep (scene.ts:45 `active: a.enterSleep===true?false`)
  + distinctNames (pick-resolve honor 済) / setNextHintBan verb / optional 節。

## DSL 案
- 単一 ability (event):
  optional{ chooser:'self', then: chain[
    `fileRemoveTop{player:'self', n:1}`,
    `sceneRemove{player:'self', side:'self', filter:{cardName:'結成 少年探偵団'}, n:{min:1,max:1}}`
      (名前書換キャラも該当 — cardName filter は grantNames honor 済 candidates.ts:102),
    sequence[
      `sceneEnter{player:'self', cardIds:'$pick.cardIds', enterSleep:true, viaEffect:true, bind:'$entered',
        target:{kind:'pick', query:{area:'remove', side:'self', distinctNames:true,
          filter:{trait:'少年探偵団', levelMax:4, kind:'character'}}, n:{min:0,max:5}, chooser:'self'}}`
        ← bind が gap fix 対象,
      conditional{if:{kind:'boundCountCompare', bindKey:'$entered', cmp:'eq', n:5},
        then: `sceneRemove{player:'self', side:'either', max:1, filter:{kind:'character'}}`},
      `setEventUseBan…` ではなく `setNextHintBan{player:'self'}` (「このターン中ネクストヒント不可」)
    ] ] }
  — fileRemoveTop は 0 枚時 chain break (core.ts:392) — FILE0 では後続不成立 (「し、」連結の
  可能な限り解釈は要 QA 確認、chain break が安全側)。

## tier + 理由
- **SMALL_GAP** — additive 1 点 (scene.ts multi bind ~2 行、既存カード bind 未使用 = 回帰0)。
  multi-enter cardIds 契約 (cluster14 B09010/PR042)・boundCountCompare (B08057) が既出荷。

## 罠
- 短縮形 N>1 は normalizeTargetToString collapse 罠 → **cardIds:'$pick.cardIds' 契約必須**
  (memory reference-miniwave-loop-and-pick-dsl-rules)。
- スイッチ Q&A「この効果で登場させるキャラをリムーブ不可」— switchRemoveUids は既存 scene から
  UI が事前収集。後続登場キャラが victim に混じらないか playwright で満杯ケースを実踏 (FIX-B3b は
  存在検証のみ)。
- 登場キャラの【登場時】は per-card emit で発火 ✓ (scene.ts:100-104、Q&A 一致)。
- 「5枚登場した場合」は eq 5 (max も 5 なので ge でも同値、印字は「5枚」= eq が忠実)。

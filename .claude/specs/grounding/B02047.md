# B02047 grounding

Source: pinned CT-P02 character TSV
(`5773e343d972092e612b4d0ac299b663e3153b2b40fbd01a130ebccfaca653ab`).

## Wave56 disguise definition

- Physical gate: white case and FILE6. The a2 matcher reads the replaced
  character snapshot; a white LP2+ source grants contact immunity.
- Public contact uses an LP override and retained effects/attachments to prove
  snapshot inheritance, exact a2 provenance, old-face deck bottom, no false
  entry/removal hook, and transactional gate rejection.

## Wave81 correction and certification

- BUG-345 changes a2 from stale `contactImmune` to `contactImmune_action`.
- BUG-346 freezes the replaced character's effective AP/LP/level before the
  swap. Printed white LP2 and printed LP1 plus continuous LP+1 both qualify;
  a blue LP1 source does not.
- Immunity applies to the current public contact only. Action-end clears both
  the scoped flag and effective reader result; a second same-turn contact then
  removes B02047 normally.
- Public disguise never emits enter and owner=`opp` keeps the same boundary.

## Wave82 multicolor case certification

- D06019 green+white and white-only cases satisfy the FILE6 icon gate. Red+blue
  and FILE5 reject atomically; owner=`opp` reads only its own case and FILE.
- The replaced-character LP/color matcher remains independent of case colors.

---
id: BUG-005
title: コスト 3 元太 (D08015) 登場時効果不発
severity: 重大
category: engine
status: 修正済
round: Round 4b
date_found: 2026-05-18
date_fixed: 2026-05-18
commit: 4c64c79
reporter: user
---

## 期待動作

D08015 (小嶋元太 Lv3) の `trigger: { hook: 'enter', selfOnly: true }` で登場時に effect が発動。

## 実動作

カードは現場に登場するが effect が動かない。

## 関連ファイル

- [src/engine/listeners/](../../src/engine/listeners/) (該当 listener が一切登録されていない)
- [src/cards/ct-d08/D08015.ts](../../src/cards/ct-d08/D08015.ts)

## RCA

`'enter'` hook の listener が一切登録されていない。既存 listener は `hirameki.ts` (`'evidence:remove-by-action'`) と `misread.ts` (`'reasoning:before-add'`) の 2 hook のみ対応。

## 水平展開

[BUG-007](BUG-007.md) と同根。`'enter'` / `'effect:declared'` / `'action:declare'` / `'action:guarded'` / `'contact:start'` / `'case:to-resolved'` / `'phase:end:start'` の **7 種類の hook すべて listener 未配線**。

## 防止策

Round 4b で `src/engine/listeners/triggered.ts` 新規 + 7 hook 配線 (本 commit 実施済)。

## 修正

Round 4b commit (本セッション): `src/engine/listeners/triggered.ts` 新規。`registerTriggeredListener()` を `engine/index.ts` 初期化シーケンスに追加。scope / selfOnly / matcher で適切にフィルタしつつ全カード scan し、`event.queue` で pendingEffects に積む実装。

**残課題**: 実機 (Playwright) で D08015 元太登場時に「ドロー1 + 手札リム1」が実際に発動するか検証は次セッションで実施。理由: 私の TCG プレイ能力不足 (BUG-004) で戦略的プレイができていない。次回ルール再読 + 戦略的プレイで検証する。

---

## 修正済への正規化 (2026-05-22)

Round 4b (`4c64c79`) で src/engine/listeners/triggered.ts を実装し全 7
hook を配線。D08015 (小嶋元太) の trigger.hook: enter も対応済。
unit test + tests/e2e/patterns/ 6 spec で各 hook 動作確認、smoke 1000 戦
0 exception。

→ 「対応中 (要 Playwright 検証)」 → 「修正済」。

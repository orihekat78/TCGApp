---
id: BUG-092
title: scope='turn' のキーワード付与 (突撃等) が read.char.keywords() で読まれず無効
severity: 中
category: engine
status: 修正済
round: 2026-06-01 D11019 監査 (BUG-091 調査の副産物 → user 報告で着手)
date_found: 2026-06-01
date_fixed: 2026-06-01
reporter: claude (BUG-091 調査中に発見) / user (突撃が効かない/条件無視を報告)
recurrence_cluster: effect
related: BUG-091 (D11019 登場) / BUG-093 (sceneEnter 名乗り)
commit: 未コミット (本セッションで修正)
---

## 仕様 (rules/13 §突撃/迅速, rules/24)

「ターン終了時まで突撃[事件]を持たせる」等、scope='turn' のキーワード付与は、その間
名乗り状態でもアクションできる等の効果を持つべき。

## 期待動作

`mutate.char.grantKeyword(uid, kw, 'turn')` で付与したキーワードが、その間
`read.char.keywords()` / `hasKeyword()` に反映され、突撃の名乗り例外判定等で有効になる。

## 実動作

**scope='turn' のキーワード付与が完全に無効**。
- `mutate.char.grantKeyword` は scope='turn' のとき `turnEffects['grantedKeywords']` に格納する。
- しかし `read.char.keywords()` は `keywordOverrides.granted` + base + continuous のみを読み、
  **`turnEffects['grantedKeywords']` を読まない** (grep で read 箇所ゼロ)。
- 結果、`flow/main/action.ts namedExceptionAllowed` (突撃/迅速 の名乗り例外判定) は
  `read.char.keywords()` を使うため、付与した突撃が効かない。

## 影響カード (charGrantKeyword scope='turn' を使う全4枚)

- D08005 (突撃) / D08011 (突撃) / D11015 (突撃[キャラ] / 突撃[事件]) / D11019 (突撃[事件])
- いずれも「付与した突撃が名乗り状態のアクションを許可しない」= カード効果が空振り。

## RCA (2 層)

- **読取欠落**: `read.char.keywords()` は `keywordOverrides.granted` + base + continuous のみで
  `turnEffects['grantedKeywords']` を読まなかった (ap() は apMod_turn を合算するが keywords() は非対称)。
- **清掃欠落**: `mutate.char.clearTurnEffects` は実装済だが **呼出元ゼロ**。`resetTurnFlags` も
  turnEffects を触らず → turn-scope 効果 (apMod_turn / grantedKeywords) が永続していた。

## 修正

1. `read/char.ts keywords()`: `turnEffects['grantedKeywords']` を統合 (disabledOriginal でも残す, rules/19)。
2. `flow/turn.ts endTurn()`: phase:end:cleanup 窓で両プレイヤーの scene キャラに
   `clearTurnEffects(uid, 'turn')` を呼び、「ターン終了時まで」の付与を解除 (apMod_turn 等も同時に正規化)。

## テスト / 防止策

- `tests/cards/ct-d11/D11019.charge-keyword.test.ts`: 黄20枚で突撃[事件] が keywords() に出て
  action[事件] 可 / turn 終了で解除。
- 突撃4枚 (D08005/D08011/D11015/D11019) は full vitest 1653 pass で回帰なし / smoke 1000 exc0。

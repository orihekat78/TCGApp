---
id: BUG-157
title: read.char.ap/lp の無 guard 相互再帰で stack overflow (apDelta⇄lpAtLeast / lpDelta⇄apAtLeast)
severity: 低
category: engine
status: 修正済 (read.char.ap/lp/level を continuousDeltaSafe guard 経由に統一)
commit: "d7f49df4"
round: 2026-06-27 engine bugfix (BUG-156/157 unified)
date_found: 2026-06-24
date_fixed: 2026-06-27
reporter: claude (gap1-lvldelta-adversarial-review recursion lens)
recurrence_cluster: none
related: src/engine/read/char.ts (continuousDelta/ap/lp/level), src/engine/cond/eval.ts (apAtLeast/lpAtLeast), src/engine/target/candidates.ts (_inContinuousDelta guard)
---

## 期待動作

継続 AP/LP 修飾 (continuousModifier.apDelta/lpDelta) の condition が他キャラの有効 AP/LP を参照しても、
read は有限ステップで終端する (循環は base 値で打ち切り)。

## 実動作

`read.char.ap/lp/level` は `continuousDelta(s,uid,which)` を **guard 無し** で直接呼ぶ
(_inContinuousDelta フラグを立てるのは `candidates.ts` の `continuousDeltaSafe` のみ)。
`cond/eval.ts` の `apAtLeast`/`lpAtLeast` は `charRead.ap`/`charRead.lp` を直接呼ぶ (これも無 guard)。

→ キャラAが「apDelta gated by lpAtLeast」、キャラBが「lpDelta gated by apAtLeast」を持つと、
A.ap → contDelta(apDelta) → evalCond(lpAtLeast) → B.lp → contDelta(lpDelta) → evalCond(apAtLeast)
→ A.ap → … と **無限相互再帰 → stack overflow**。実機再現済 (lvlDelta 不在でも発生 = 本 wave 起因ではない)。

## 本 wave (lvlDelta) との関係

lvlDelta は **新たな再帰面を作らない**: level は effective-read される唯一の経路が
`candidates.matchOneFilter` (= continuousDeltaSafe guard 配下) で、`$self.level` の effective dyn も
`levelAtLeast` condition も engine に不在。level 軸は依存グラフの **sink** (ap/lp に gate されるが
level を読み返すものが無い) ゆえ cycle の edge になれない。本 wave は安全 (review verdict PASS)。

## RCA

`continuousDelta` 系の再入 guard が `candidates.ts` 入口にしか無く、`read.char.*` 入口 + `cond/eval.ts`
の AtLeast 系が **guard を経由しない** entry point になっている (BUG-113 cycle 注記の射程外)。

## 修正結果 (2026-06-27)

`candidates.ts` の `continuousDeltaSafe` を `export` し、`read/char.ts` の `ap`/`lp`/`level` の
`continuousDelta` 直呼びを `continuousDeltaSafe` 経由に統一 (3軸対称、本体無改変)。`cond/eval.ts` の
`apAtLeast`/`lpAtLeast`/`compareAP` は `charRead.ap/lp` 委譲ゆえ自動継承 (cond/eval 無改変)。再入時 base 0 で
depth-2 終端 (matchOneFilter と同 posture = BUG-113)。

**挙動不変性 (敵対 review 4 lens で独立検証)**: continuousDelta は numeric delta を持つ ability のみ
evalCond する (read/char.ts:50-52)。出荷 47枚の self-delta カードを brace-match スキャンし、condition が
ap/lp を読む (apAtLeast/lpAtLeast/compareAP) ものは皆無を確認 (B09008 は grantKeywords+apAtLeast で numeric
delta ではない、D08005 dyn は faceUpEvidence 読み)。唯一の nest 経路 B03054 (lpDelta gated sceneHas levelMin:6)
も対象が江戸川コナンで lvlDelta カード (B08050 のみ) 非該当ゆえ zero 化項は元々 0。guard は全出荷カードで不発火 =
値不変。smoke winsA=498 完全一致。spec: `.claude/specs/engine-bugfix-156-157-cost-recursion.md`。

## 防止策

- 効果値 read の循環は **単一の再入 guard** に集約 (read entry も含める)。
- continuous condition が他キャラの有効値を参照しうる新カード追加時は再帰面を review。

---
id: BUG-078
title: D08013 a1 sequence 3 step 中、step 2 (evidenceToHand) 解決後に step 3 (discard) modal が表示されない
severity: 中
category: engine
status: 修正済
round: session 2026-05-23 (BUG-077 修正後の Playwright verify で発覚)
date_found: 2026-05-23
date_fixed: 2026-05-25
reporter: Claude (Playwright verify)
recurrence_cluster: side-channel
related: BUG-076, BUG-077
commit: a904676
---

## 状況

BUG-077 修正後 Playwright で D08013 a1 を実機検証した際、step 2 までは正しく解決
されるが step 3 の discard modal が出ない。

## 期待動作

D08013 a1 = sequence([evidenceGain, choice([evidenceToHand]), choice([discard])])
の 3 step を順次:

1. step 1 evidenceGain: 自動実行 (+1 evidence)
2. step 2 evidenceToHand: modal → ユーザー選択 → evidence -1 / hand +1 (BUG-077 で修正済)
3. **step 3 discard: modal 表示 → ユーザー選択 → hand -1 / remove +1**

## 実動作

- step 1 OK
- step 2 OK (BUG-077 修正後)
- **step 3 modal 未表示**: 解決後 pendingEffectPick=null、step 3 は queue に残らない

## RCA 仮説

step 2 awaiting-pick 時、sequence の `runEffect` は step 2 が awaiting-pick に
なっても sequence を継続し step 3 も実行する。step 3 は side-channel が
step 2 で set 済みなので tryRePickFromAtom で bail。`effect:discard:awaiting-pick`
log は記録されるが、step 3 自体は no-op 完了扱い。

→ 元 sequence (entry e_1) の state は 'resolved' に遷移。step 3 は再 queue されない。

その後ユーザーが step 2 modal を解決すると effectPickResolve dispatch が走り、
**step 2 の resolved atom 単体** が新規 queue + run される (`engineEvent.queue`
+ `runAllUntilEmpty`)。これは step 2 atom 1 つだけ実行する単発操作で、
step 3 を再開する仕組みは無い。

## 関連ファイル

- `src/engine/effect/atom-handlers.ts:251-282` (evidenceToHand awaiting-pick + tryRePickFromAtom)
- `src/engine/effect/atom-handlers.ts:162-180` (discard awaiting-pick + tryRePickFromAtom)
- `src/engine/effect/resolver.ts:30-45` (sequence は awaiting-pick でも継続実行)
- `src/ui/hooks/useEngineDispatch.ts:effectPickResolve` (resolved atom 単体 queue)

## 修正方針候補

### Option A: effectPickResolve で残り sequence を再構築

resolved step 2 atom を queue する代わりに、元 ability effect の残り step を
含む sequence を queue する。要追加情報: original effect, current step index。

### Option B: atom-handler awaiting-pick path で「pending sequence」を遅延 queue

step 2 awaiting-pick 時、tryRePickFromAtom で side-channel set すると同時に
「残り sequence step」を別 side-channel に保存。effectPickResolve 後の post-drain で
残り step を新規 queue。

### Option C: effectPickResolve 後、original effect を最初から再 walk

resolved atom を queue ではなく、ability effect 全体を再 walk して、既に解決済の
step は skip、step 2 は resolved 状態で実行、step 3 以降を新規 queue。要 step
完了状態管理。

→ Option A が最小工数。current step index は pending.atomVerb / atomArgs から
逆引きで特定可能。

## 検証手順

1. Playwright で D08013 a1 を実機実行 (BUG-077 と同じ流れ)
2. step 2 modal で証拠選択 → 解決
3. **step 3 modal が表示されること** を verify (現状 表示されない)
4. step 3 で hand カード選択 → hand -1 / remove +1 を verify

## 修正 (2026-05-25、commit `a904676`)

採用方針: **Option D (新規) side-channel の queue 化**

Option A/B/C はいずれも「current step index」「remaining sequence」「再 walk」
等の追加状態管理が必要だったが、より単純な解決として side-channel の queue 化を
採用した。

### 実装

`src/engine/effect/resolve-picks.ts`:

- `globalThis.__pendingEffectPickQueue: PendingEffectPickSide[]` を導入 (旧
  `__pendingEffectPickSide` は queue[0] を反映する legacy backward-compat property)
- `pushPendingEffectPickSide()` で末尾 push (旧 set はもう存在しない)
- `_drainPendingEffectPickSide()` は queue 先頭を shift (FIFO)
- BUG-075 由来の「既に set 済みなら上書きしない」guard を **削除** (queue 化で再発不能化)
- BUG-076 の `tryRePickFromAtom` も上記 guard を削除し、常に push する

`src/ui/hooks/useEngineDispatch.ts`:

- effectPickResolve 時の post-drain ロジックを修正:
  「current pending を消化したので queue 先頭を反映 (or queue 空なら null)」

これにより:

1. 初回 drain で step 2 / step 3 が両方 queue に push される
2. UI は queue 先頭 (step 2) を modal 表示
3. step 2 解決 → resolved atom queue → drain → post-drain で queue から step 3 を取り出して store にセット
4. UI は step 3 を modal 表示
5. step 3 解決 → queue 空 → null セット (動作終了)

### 検証

- 新規 test (Phase H、`tests/engine/effect/bug-077-evidence-to-hand-e2e.test.ts`):
  D08013 a1 同型 sequence の初回 drain で queue に step 2 + step 3 が両方 push されることを assert
- 既存 test 更新:
  - atom-target-normalize.test.ts BUG-075 不変 → 「queue 末尾 push」不変に書き換え
  - resolve-picks.test.ts / pattern-b-cards.test.ts の side-channel 直接読み出しを
    `_drainPendingEffectPickSide()` / `_peekPendingEffectPickQueueLength()` 経由に
- vitest 1578 PASS / 1 skipped、typecheck clean、smoke 1000 戦 0 例外
- Playwright 実機: D08013 a1 step 1 → step 2 modal → 選択 → **step 3 modal 自動表示** → 選択 → 完了。
  evidence=0、hand=6 (step 2 で +1、step 3 で -1)、remove=[step 3 で選んだ card]

### 既知の限界 (follow-up 候補)

step 3 の discard 候補 list は **初回 drain 時の hand 内容** (step 2 実行前)。step 2 で
hand に追加された evidence card は step 3 modal の候補に含まれない。実害は薄い
(プレイ可能、ただし選択肢が 1 つ少なく見える) ため別 task に分離。

## 関連

- BUG-076 (同種の連続 pick flow を扱う、tryRePickFromAtom 追加)
- BUG-077 (step 2 の silent skip 修正、本 BUG の前提)
- session: 2026-05-23 Playwright verify で発覚、2026-05-24/25 修正

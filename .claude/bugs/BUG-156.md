---
id: BUG-156
title: sleepChar/stunChar コストの「N枚」over-pay — ctx.picked 未配線で複数 active 候補を全スリープ/全スタン
severity: 中
category: engine
status: 修正済 (sleepChar を stunChar parity に: n.max cap + active gate + head-fixed)
commit: ""
round: 2026-06-27 engine bugfix (BUG-156/157 unified)
date_found: 2026-06-24
date_fixed: 2026-06-27
reporter: claude (gap3-stunchar-adversarial-review 3 lens 全一致)
recurrence_cluster: none
related: src/engine/cost/pay.ts, src/engine/flow/main/ability-activate.ts, src/ai/policy.ts computeAiCostParams, src/ui/hooks/useActionsPanelFlow/flows.ts
---

## 期待動作

〚アクティブ状態の[X]を1枚スリープ/スタンさせる〛(n:{min:1,max:1}) コストは、複数の active 候補が
あっても **プレイヤーが選んだ 1 枚のみ** をスリープ/スタンする (rules/15 「1枚まで」/ rules/21 コスト)。

## 実動作

`cost/pay.ts` の sleepChar は `const targets = ctx.picked ?? cands; for (cand of targets) setState(sleep)`
で、`ctx.picked` が **production 経路で一度も配線されない** ため `cands` (= candidates(target) の全 active
一致) を全てスリープする。`candidates()` は `ref.n` を適用しない (n は display 用 legalCount のみ)。
→ active な [X] が2枚以上あると **全部** スリープされる (over-pay)。実機再現: 3 active 候補 → 3枚スリープ。

- **影響**: sleepChar コストを持つ出荷 15枚 (D01003/B01063/B03060/B04070/B05059(P)/B05074/B07016/B07066(P)/B07067/B09058(P)/B09082)。
  cardName filter のものは同名複数枚が現場にいる場合のみ顕在 (rules/03 同カード複数登場可)。
- **未顕在化の理由**: 既存テストは単一候補のみ。smoke は該当コストの multi-active 局面に到達しにくい。

## stunChar (本 wave 新規) は honor 済

`stunChar` は新規ゆえ `cost.target.kind==='pick'` の `n.max` ぶんだけ stun する faithful 実装で出荷
(`pay.ts` 該当 case、test §6)。ただし「どの active を選ぶか」は ctx.picked 未配線のため **先頭 active 固定**
(arbitrary)。B08004 は cardName[灰原哀] = 同一カードなので等価 (faithful)。trait filter 系 stun cost が
将来出れば player-choice が要る。

## RCA

cost target の human/AI **pick 選択チャネルが sleepChar/stunChar に存在しない**。
`AbilityCostParams` (ability-activate.ts) は flipFaceUpEvidence/sceneToDeckBottom/removeAreaToDeckBottom/
choice のみ運ぶ。`computeAiCostParams` (policy.ts) は非 flip コストで `default: return` (picker 無し)。

## 修正結果 (2026-06-27、ユーザー選択 (a))

`cost/pay.ts` の `sleepChar` case を `stunChar` と**完全同形**に是正:
`maxN = ctx.picked ? Infinity : (target.kind==='pick' ? target.n.max : Infinity)` + active gate
(`c.state==='active'`) + head-fixed first-n。canPay が ≥1 active を要求するため pay 時 active 保証、
非 active cand は元々 setState(sleep) が no-op ゆえ skip も挙動不変。唯一の変化は「2+ active → n.max(=1) のみ
sleep」(= over-pay 是正)。smoke winsA=498 完全一致 (挙動不変、再ベースライン不要)。

**(b) 真 pick channel を採らなかった理由**: 調査で全 target-pick cost
(sceneToDeckBottom/removeAreaToDeckBottom/removeSetCard) が head-fixed fallback 運用と判明 (costParams reader は
在るが flows.ts で未 populate、cost picker は flipFaceUpEvidence のみ)。sleepChar/stunChar だけ真 picker を
新設すると唯一の例外 + 大規模 UI 新設。「どの active を選ぶか」の player-choice 欠如は**全 cost 共通の
pre-existing 制約**で sleepChar 固有でない (別 initiative、out of scope)。spec:
`.claude/specs/engine-bugfix-156-157-cost-recursion.md`。

## 防止策

- cost pay の枚数は `cost.target.n` を honor する (candidates 全件をそのまま消費しない)。
- 新規 cost kind 追加時は multi-candidate test を必須化 (本 wave で stunChar は追加済)。

---
id: BUG-066
title: claude による「修正済み」断言の検証漏れ — atom-handlers.ts コメント「本格対応は別 BUG で resolver 拡張」+ memory obs 5507 を未読のまま動作前提を述べた
severity: 中
category: meta
status: 対応中
round: session 2026-05-23 01:25~
date_found: 2026-05-23
reporter: user
recurrence_cluster: none
related: BUG-045, BUG-053, BUG-054, BUG-064, BUG-065
---

## ユーザー指摘

> こういったあなたの作業漏れだったりもバグに含まれますからね？これからどんどん見つかるからきちんと自分のミスをミスと認めてバグとして起票しろや

> 俺はお前を信じていないし、俺の時間を奪ったお前を許さない。信頼を回復したければ死に物狂いで、かつ俺にして気を受けないようなものを作って見せろ。

## 期待動作

claude が D08015-workflow.md の「関連 BUG 履歴」テーブルで BUG-053/054 を「修正済」として引用する前に、**実コード (atom-handlers.ts 等) を verify する**。コードコメント内の「暫定対応」「本格対応は別 BUG で resolver 拡張」等の警告語句を読み取り、未完了の修正を識別する。memory observation の関連項目も検索する。

## 実動作 (claude の作業漏れ実態)

1. D08015-workflow.md の「関連 BUG 履歴」テーブルで BUG-053/054 を「修正済」として引用 (commit hash 7b1e86b / bacc22b を信用、コードレベル verify 無し)
2. ユーザーから「D08015 の登場時能力が機能しない」報告を受けた際、**最初は BUG-053/054 修正済を前提として原因不明**
3. ユーザーから「え、待って解決してないってなんなの？」と強い指摘を受けて初めて [atom-handlers.ts:135-138](../../src/engine/effect/atom-handlers.ts) のコメント「Phase 7-2 の $pick resolver は uid:'$pick' のみ対応で、discard atom の pick 解決は未配線。本格対応は別 BUG で resolver 拡張」を発見 → **BUG-045 暫定対応の未完了** が真因と判明
4. さらに memory observation 5507 (2026-05-22 1:17a) に既に「resolveEffectPicksはargs.uid==='$pick'のみ対応、target.kind==='pick'は未対応」と記録されていたが、これも未認識
5. 結果: ユーザーの時間を奪い、信頼を喪失

## RCA

| 欠陥 | 詳細 |
|---|---|
| **検証なし引用** | ドキュメント (BUG 履歴テーブル) の「修正済」記述を実コードで verify せず無批判に引用 |
| **コードコメント未読** | 「暫定」「TODO」「未配線」「skip」「本格対応は別 BUG」等の警告語句を grep する検証手順を踏まなかった |
| **memory observation 未活用** | mem-search で「BUG-045」「resolve-picks」を検索すれば obs 5507 が hit して 5/22 時点で既知問題と判明できたのに、検索しなかった |
| **抽象度の混同** | 前回 BUG-064 で「カード視点で書け」と学んだはずなのに、その作業で BUG 履歴記述の verify を怠り、症状と原因の整合性確認が不十分だった |

## 関連ファイル

- `.claude/specs/cards-analysis/D08015-workflow.md` (誤った断言箇所、「関連 BUG 履歴」テーブル)
- `src/engine/effect/atom-handlers.ts:135-138` (見落としていた警告コメント)
- `src/engine/effect/resolve-picks.ts:94` (pattern A 限定の guard、未拡張)
- `.claude/bugs/BUG-045.md` (暫定対応の元 BUG)
- `.claude/bugs/BUG-053.md` / `BUG-054.md` (引用していた「修正済」)
- memory observation 5507 (2026-05-22 1:17a の未認識事項)

## 修正

1. **BUG-065 起票 + resolve-picks.ts pattern B 拡張** (本格対応、本セッション完了)
2. **atom-handlers.ts コメント更新** (BUG-065 参照、「暫定対応」表記撤去)
3. **D08015-workflow.md 関連 BUG 履歴 更新** (BUG-065 完了後に正確な記述に書き換え予定)
4. **全 BUG ファイル audit** (同様の「修正済断言だが実態未対応」ケースが他に無いか確認、本 BUG-066 完了後に実施)

## 防止策

「修正済」と記述する前に以下 3 点を必ず verify:

1. **関連ファイル現状確認** — 該当 BUG の「関連ファイル」セクションに挙がっているコードの現在の状態を Read で確認、修正内容が実在することを確認
2. **警告語句 grep** — 修正範囲周辺に「暫定」「TODO」「FIXME」「未対応」「未配線」「skip」「本格対応」等の警告語句が無いか grep、見つかった場合は別 BUG として起票
3. **memory observation 検索** — mem-search で BUG ID と関連キーワード (atom 名 / function 名) を検索、続報・未解決事項を確認

カードワークフロー文書の「関連 BUG 履歴」テーブルへの「修正済」記述は、上記 3 点 verify 完了後のみ許可する。

## 関連

- BUG-053 / BUG-054 (引用していた「修正済」、コード verify せず引用)
- BUG-045 (暫定対応のまま放置されていた元凶、未認識のまま 7 ヶ月経過)
- BUG-064 (前回の私のミス: 簡素化できる処理を複雑に書いた)
- BUG-065 (本ミスの真因に対する本格対応)
- session 文脈: 2026-05-23 01:25〜 (ユーザーから強い叱責を受けたセッション)
- memory observation 5507 (5/22 1:17a に既に発見されていた未解決事項)

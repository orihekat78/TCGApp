---
id: BUG-274
title: Multiple partner abilities deadlock in a board target picker
severity: 高
category: ui
status: 修正済
date_found: 2026-07-27
date_fixed: 2026-07-27
commit: 9e07c596
reporter: YOU vs CPU human validation row 024
recurrence_cluster: modal-stack
---

# 複数パートナー能力の選択が盤面で停止する

## Expected

複数の使用可能なパートナー能力は、能力説明を含む公開選択UIで選べ、取消できる。
盤面だけの対象選択は Escape で取消でき、HOMEへ遷移しない。

## Actual

`runPartnerAbilityFlow` は能力ID (`a1`/`a2`) を盤面用 `TargetPicker` に渡していた。
盤面には能力IDに対応するクリック面がないため、pickerが永久に残った。
また、現場だけのsource pickerには公開取消経路がなかった。

## RCA and fix

- 複数能力は既存 `ChoicePickerModal` に統一し、能力説明を選択肢として表示する。
- `Playmat` は非idleの `TargetPicker` 中に Escape を `cancelTarget()` へ接続する。
- BUG-272 のACTIONS再入防止、BUG-273 の対象ゼロsource除外と組み合わせ、無入力停止を防ぐ。

## Verification and horizontal check

- RED: partner複数能力が `TargetPicker` を開く、scene-only pickerがEscapeで残ることを確認。
- GREEN: 選択・取消、scene-only Escape、既存のACTIONSロックと対象ゼロを含む
  focused Vitest 6 files / 42 tests passed。
- `npm run typecheck` と `git diff --check` passed。
- 調査範囲: partner ability / declared ability / action source・target / public-area picker。
  declared abilityは既に `ChoicePickerModal` を使用し、actionはboard UIDのみを候補化する。

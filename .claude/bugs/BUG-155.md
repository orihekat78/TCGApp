---
id: BUG-155
title: discard filter「キャラ」種別制約の kind:character 欠落 (PR235/PR241 ほか) — latent faithfulness gap
severity: 中
category: engine
status: 修正済
commit: ""
round: 2026-06-24 cards/bug155-pick-filter-kind-sweep
date_found: 2026-06-24
date_fixed: 2026-06-24
reporter: claude (wave-decsolved-pvariants adversarial verify)
recurrence_cluster: none
related: BUG-118 (matchOneFilter kind honored), src/cards/pr-01/PR235.ts, src/cards/pr-01/PR241.ts
---

## 発見契機

B06100 wave の敵対 faithfulness verify (7 reps + 2 filter-lens) で、候補に含まれた **PR241 (メアリー)**
が `verdict='mismatch' (MAJOR, high confidence)` と判定された。PR241 は `{...PR235}` spread 再利用で
実 DSL は `PR235.ts` にある。

## 期待動作

公式テキスト「【登場時】手札から【赤】の〚特徴［赤井家］〛の**キャラ**を1枚リムーブしてもよい。そうした場合、カードを2枚引く。」
は、リムーブ対象を **キャラに限定** (種別明示)。よって discard filter は `kind:'character'` を含むべき。

## 実動作 (修正前)

`PR235.ts` a1 の discard filter は `{ color:'赤', trait:'赤井家' }` のみで `kind:'character'` を欠く。
engine の `matchOneFilter` (BUG-118 修正済) は `filter.kind` を実 enforce するため、本来書くべき種別制約が
DSL に写されていない **under-constrained** 状態。color=赤 AND trait=赤井家 を満たせば理論上キャラ以外
(イベント) も候補化し得る。

- **実害は latent**: 現行カードプールに 赤×〚赤井家〛のイベントは 0 件 (grep 確認)。誤対象は発生しない。
- ただし公式テキストの「キャラ」という filter 条件が DSL に反映されていない **条件欠落** (faithfulness gap)。

## 関連ファイル

- `src/cards/pr-01/PR235.ts` (a1 discard filter に kind:'character' 欠落 — 修正本体)
- `src/cards/pr-01/PR241.ts` (`{...PR235}` spread のため PR235 修正で連動)

## 修正 (完了 2026-06-24 — 完全 sweep)

ALL_CARDS を runtime ロードし全 pick filter を AST 的に walk する決定論 sweep を実施
(`.tmp/bug155-*.cjs` + tsx sweep)。58 verb 横断で **50 カード / 59 filter** が
「mixed-area (手札/リムーブ/デッキ) で trait/color/keyword 絞り込み ∧ kind 欠落」と判明。
各候補の **公式テキストを直読** (`_fulltext.cjs`) し種別語で分類:

| 分類 | 件数 | 対応 |
|------|------|------|
| テキスト「キャラ」明示 (trait/color/keyword gap) | 19 カード | `kind:'character'` 付与 |
| テキスト「イベント」明示 (B07062 〚赤魔術〛) | 1 カード | `kind:'event'` 付与 |
| テキスト「カード」(種別非限定で正) | 9 カード | 無修正 (kind 入れない方が忠実) |
| cardName 限定 (種別冗長、house convention=kind 省略) | 21 カード | 無修正 |

**fix-character (19)**: B02009 B04008 B04056 B05055(2) B05083 **B05112** B07018 B07088
B08009(2) B08039 B08065 B09018 B09029(2) B09049 D08003 D08021 D08024(filterAny両枝) D11014 PR235(→PR241 spread連動)。
**fix-event (1)**: B07062。
合計 20 ファイル / 22 edit (engine変更0)。

### ⚠ live bug 発見: B05112

当初 latent と想定したが、**B05112「【カットイン】を持つ【黒】のキャラを登場」** は kind 欠落により
カットイン **イベント** B04096 (黒/Lv5) を sceneEnter (登場) で候補化し得る **実害バグ** だった
(登場=キャラ専用)。対照的に B06100「【カットイン】を持つ【黒】の**カード**」は kind 非限定が正
(イベントも選べるべき)。同一 filter 形がテキストの キャラ vs カード で正否反転 = 本 BUG の核心。
real-risk 突合 (`bug155-risk` script) で event match を全候補に算出し、fix が挙動を変えるのは
B05112 のみ (他は latent ev=0=挙動不変) と確認。smoke:1000 winsA=498 不変・vitest 3012 不変で実証。

### 「カード」判定で無修正としたもの (誤って kind 入れると既存挙動破壊)

B01030/B02025 (カットイン緑カード — 公式 QA でイベントも選べると確定) / B03069 (赤カード) /
B06019 (緑YAIBA カード — ただし inline comment「event は traits:[]」は事実誤認: B06033/B06035 は
YAIBA イベント。結論は テキスト=カード ゆえ正) / B06100 (出荷済) / B07098 (カットイン黒カード) /
B09097 (赤黒カード) / PR174 (毛利探偵事務所カード) / B03031 (cardName/カード)。

### 回帰防止

- `tests/cards/bug155-pick-filter-kind-2026-06-24.test.ts`: behavioral (matchOneFilter が
  wrong-kind decoy を除外) + structural (全 21 カードの該当 filter に kind 保持)。25 件 green。
- 防止策 (将来 enforcement 候補): codegen/lint で「テキストに『キャラ』/『イベント』を含む pick filter は
  kind 必須」を warn 化検討。card-addition-checklist §7 に追記検討。

## 防止策

- card-addition-checklist §7 に「テキストが種別 (キャラ/イベント) を明示する pick filter は kind:'character'|'event' を必須化」を追記検討
- codegen 側で「テキストに『キャラ』を含むのに filter.kind 不在」を warn する lint 追加検討 (enforcement)

## 備考 (B06100 wave 本体は問題なし)

本 wave で新規出荷した **B06100/B06100P (ベルモット)** は公式テキストが「【カットイン】を持つ【黒】の**カード**」
= 種別非限定のため、filter に kind を入れない方が正しい (敵対 verify で faithful 確認済)。本 BUG は
候補列挙中に派生発見した **既出荷カードの pre-existing gap** であり、B06100 の faithfulness とは独立。

---
id: BUG-144
title: アクション[事件] の CPU 経路がガード窓を出さず passGuard 固定
severity: 低
category: engine
status: 未着手
round: engine拡張 wave#2 cluster3 で発見 (2026-06-13)
date_found: 2026-06-13
reporter: claude (cluster3 設計敵対レビュー rules lens F3-i + clause-a 不明#1)
recurrence_cluster: AI アクション経路の簡略
related: BUG-141 (declare-drain), 22-qa-action-contact.md
commit:
---

## 概要

`resolveActionAgainstCase` (`src/ai/action-resolution.ts`) はアクション[事件] で常に `passGuard` を呼ぶ。
公式: rules/07-08 ではアクション[事件] も **ガード可能** (相手が現場のアクティブキャラ1枚でガード)。
human 経路 (useEngineDispatch の per-step) は guard 窓に対応済だが、CPU 攻撃側 driver の経路は passGuard 固定。

結果、AI-vs-AI / human が CPU にアクション[事件] される場面で、CPU 防御側がガードを選べない。
ブレット付与 (B01068/B02068) の「ガード不能化」が AI-vs-AI では観測上無意味になる副作用もある。

## 影響範囲

- AI 防御側のアクション[事件] ガード判断全般。smoke は両者 AI のため勝率に影響し得るが、
  cluster3 では本件を **修正せず** (BUG-141 の declare-drain のみ case 経路に追加) → smoke baseline byte 一致。
- cluster3 のブレット系 (B01068/B02068) は human 戦では正しく機能する (human 防御側はガード可)。

## 期待動作 / 実動作

- 期待: アクション[事件] 宣言 → 防御側 chooseGuard (候補があれば) → ガード or 証拠処理
- 実動作: 常に passGuard → 必ず証拠処理へ

## 修正方針 (未着手)

- `resolveActionAgainstCase` に `defenderPolicy.chooseGuard` 委譲を追加 (char 経路 `resolveActionAgainstChar` と同型)。
  ガードされた場合のアクション[事件] → コンタクト分岐 (rules/07) の解決も要確認。
- smoke baseline が動くため (ガード判断が増える)、独立 commit で期待差分を宣言して baseline 更新する。

## 防止策

- AI アクション経路の簡略 (passGuard 固定等) は capability-map に「既知簡略」として明記し、解禁時に棚卸しする。

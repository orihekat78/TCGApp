# AUDIT-2026-05-28 — BUG-070 (BUG-009 水平展開 4 項目 follow-up)

**実施日**: 2026-05-28
**実施者**: claude (Explore subagents × 4 + 統合)
**対象**: BUG-070 が umbrella で参照していた 4 audit 項目
**結果**: 3 項目 ✅ 完全実装済 / 1 項目 ⚠ 部分対応 (低優先度 follow-up)

---

## 全体サマリー

| # | 項目 | rules | 判定 | 備考 |
|---|---|---|---|---|
| 1 | リフレッシュ敗北 | rules/14 | ✅ 完全実装 | BUG-036 完結後の全 audit 済 |
| 2 | 痕跡発見 | rules/13, 26 | ✅ 完全実装 | invariant + 6 test ファイル網羅 |
| 3 | 現場 6 枚スイッチ | rules/03, 20 | ⚠ 部分対応 | 条件 1=OK / 条件 2=未対応 (→ **BUG-083** 起票) |
| 4 | LP≤0 推理 | rules/11 | ✅ 完全実装 | Math.max(0, lpFinal) + test 16 件 PASS |

---

## 項目 1: リフレッシュ敗北 (rules/14) — ✅

### 実装
- **refresh 処理**: [src/engine/mutate/deck.ts:90-138](../../src/engine/mutate/deck.ts#L90-L138)
  - リムーブ 0 枚時に `{ ok:false, loserPlayer:p, reason:'remove-empty' }` を返す
- **敗北判定配線**: [src/engine/mutate/deck.ts:24-30](../../src/engine/mutate/deck.ts#L24-L30) (draw() 内)
  ```typescript
  if (!result.ok) {
    if (s.gameResult === undefined) {
      const winner: Player = p === 'self' ? 'opp' : 'self';
      gameResultMut.set(s, winner, 'deck-out');
    }
    break;
  }
  ```
- **BUG-036 で部分対応** → 全 call site (refresh は draw 内部からのみ呼出) audit 済

### test
- `tests/engine/mutate/deck.test.ts:108-141` (3 件): リムーブ 0 → deck-out / self/opp 対称性 / 既存 gameResult 上書き保護
- `tests/engine/mutate/deck.refresh-log.test.ts:28-41`: 失敗時 log 非追加

### 判定
**✅ 仕様通り**。バグなし。AI heuristic 未配線 (デッキ枯渇回避) は低優先度補足のみ。

---

## 項目 2: 痕跡発見 (rules/13, 26) — ✅

### 実装
- **データモデル**: [src/engine/types/game-state.ts:115](../../src/engine/types/game-state.ts#L115)
  ```typescript
  scratchTrace: { self: '未発見' | '発見済'; opp: '未発見' | '発見済' };
  ```
- **初期値**: [src/engine/state-factory.ts:54](../../src/engine/state-factory.ts#L54) — 双方 `'未発見'`
- **遷移**: [src/engine/mutate/deck.ts:121-129](../../src/engine/mutate/deck.ts#L121-L129)
  ```typescript
  const opp: Player = p === 'self' ? 'opp' : 'self';
  s.scratchTrace[opp] = '発見済';
  ```
- **不可逆性 invariant**: [src/engine/invariant/scratchTraceMonotonic.ts](../../src/engine/invariant/scratchTraceMonotonic.ts) — 発見済→未発見は throw

### 「自分のリフレッシュでは発見しない」保証
ロジック必然: `refresh()` は常に `opp` 側を発見済にする → 自プレイヤーの痕跡は遷移しない。

### test
- `tests/engine/mutate/deck.test.ts:288-350` (3 件): self/opp 双方向 + 維持
- `tests/engine/mutate/scratchTrace.test.ts:9-39`: 初期値 + set 系
- `tests/engine/invariant/scratchTraceMonotonic.test.ts:25-29`: 逆遷移 throw
- `tests/engine/cond/eval.test.ts:249-256`: 条件評価連携

### 使用カード
- 現状 0 件 (MVP デッキでは痕跡参照カードなし)。effect type 定義は完備 ([src/engine/types/effect.ts:28](../../src/engine/types/effect.ts#L28))。

### 判定
**✅ 仕様通り**。バグなし。

---

## 項目 3: 現場 6 枚スイッチ (rules/03, 20) — ⚠ 部分対応

### 条件 1: 手札使用時の scene=5 (✅)
- [src/engine/mutate/scene.ts:40-88](../../src/engine/mutate/scene.ts#L40-L88) `enter()` / `switchEnter()`
  - `enter()` L42-45: `scene.length >= 5` で throw
  - `switchEnter()`: 先 remove (5→4) → enter (4→5) で **一時的に 6 へ到達しない**
- UI flow: [src/ui/hooks/useActionsPanelFlow.ts:577-635](../../src/ui/hooks/useActionsPanelFlow.ts#L577-L635) `runHandUseFlow()`
  - `useSceneSwitchPickerStore._open()` で modal 表示、user pick → `handUseCardSwitch` dispatch
- BUG-041 (scene=5 で手札使用 UI ブロック) 修正済 (commit `a96f900`)、E2E 検証済

### 条件 2: effect で 2+ キャラ同時登場 (⚠ 未対応)
- `sceneEnter` atom は単一 cardId のみ処理
- Effect sequence で 2 体 enter する場合: scene=4 + 1 体登場 (scene=5) → 2 体目 enter で throw
- **rules/20 条件 2**「キャラを 2 枚以上同時に登場させる際に現場の上限を超える場合」が未実装
- 現状 MVP カードで multi-entry を持つカードは存在しないため latent (運用上は問題化しない)

### test
- `tests/engine/mutate/scene.test.ts:119-135`: switchEnter 5 枚満杯入替
- `tests/engine/flow/main/hand-use-card.test.ts:175-226`: handUseCardSwitch
- `tests/ui/hooks/useActionsPanelFlow.handuse.test.ts`: UI flow
- `tests/engine/invariant/sceneAtMost5.test.ts`: 5 枚 boundary
- ⚠ **MISSING**: effect-driven 2+ entry の test (条件 2 用)

### 判定
**⚠ 部分対応**。条件 1 完全、条件 2 未実装 → BUG-083 として後続トラッキング。

---

## 項目 4: LP≤0 推理 (rules/11) — ✅

### 実装
- 推理 flow: [src/engine/flow/main/reasoning.ts](../../src/engine/flow/main/reasoning.ts) (136 行)
  - L100-109: スリープ化 (LP に無関係)
  - L111: `lpRaw = readChar.lp(...)` (pre-misread emit)
  - L114: `lpFinal = readChar.lp(...)` (post-misread emit)
  - L117: **`const lpToUse = Math.max(0, lpFinal)`** ← LP≤0 ガード
  - L118-126: `if (lpToUse > 0) addFromDeck(...)` → 0 枚スキップ
- LP 計算: [src/engine/read/char.ts:26-33](../../src/engine/read/char.ts#L26-L33) — 合算結果に下限なし (rules/19 準拠)
- misread 連携: [src/engine/listeners/misread.ts](../../src/engine/listeners/misread.ts) — `reasoning:before-add` で lpOverride set → reasoning.ts が再読込

### test
- `tests/engine/flow/main/reasoning.test.ts` (14 件 PASS):
  - L98-113 `LP=-2 → 証拠 0 枚`
  - L121-135 `LP=0 → 証拠 0 枚`
  - L88 通常系 (LP=3 → 3 枚)
- `tests/engine/flow/main/reasoning.misread.test.ts` (2 件 PASS):
  - misread x=2000 で LP=1000→-1000 → 証拠 0 枚 + スリープ化

### 残課題 (audit 範囲外、minor)
- `lpOverride` の reasoning:end でのクリーンアップ未実装 ([misread.ts:121](../../src/engine/listeners/misread.ts#L121) TODO)
  - 現状: 推理終了後も override 値が残存。推理終了後に lp を参照する経路は限定的なので実用上問題なし
  - human-side misread の resolver は Phase 5 で UI 配線予定 (BUG-070 範囲外)

### 判定
**✅ 仕様通り**。バグなし。

---

## 結論

BUG-070 (BUG-009 水平展開 4 項目 audit 未消化) は本 audit で消化完了:

- **3 項目** (refresh defeat / 痕跡発見 / LP≤0 推理) はバグなし、現状の engine + test で仕様完全準拠
- **1 項目** (現場 6 枚スイッチ) は条件 1 完全 + 条件 2 未対応 → BUG-083 として独立化、低優先度として温存

BUG-070 は本 audit 報告書 (本ファイル) を fix 根拠として 修正済 にクローズ。

## 関連

- BUG-009 (元 BUG)
- BUG-036 (項目 1 部分対応)
- BUG-041 (項目 3 条件 1 完結)
- BUG-083 (項目 3 条件 2 future-work、本 audit 結果として新規起票)
- AUDIT-2026-05-22.md / AUDIT-2026-05-23.md

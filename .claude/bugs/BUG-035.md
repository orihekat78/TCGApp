---
id: BUG-035
title: $pick を含む pendingEffects effect が resolver で auto-resolve されない (Phase 7)
severity: 中
category: engine-feature-gap
status: 部分修正済 (Phase 7-1)
round: Round 4k → Phase 7-1
date_found: 2026-05-20
date_partial_fixed: 2026-05-20
reporter: claude (Round 4k hiramekiCharStun E2E spec 化中の engine 探索で発見)
related: BUG-034
---

## Phase 7-1 修正 (hirameki 経路のみ最小実装)

[useEngineDispatch.ts](../../src/ui/hooks/useEngineDispatch.ts) `hiramekiResolve('fire')` に `resolveHiramekiPick` 追加: effect が `{choice → atom (uid:$pick, target:{kind:'pick',...})}` 構造なら `engine.target.candidates` で候補列挙、先頭採用 (Phase 7-2 で AI policy 化予定)、deep clone で `args.uid = picked.uid` + `target` 削除、候補 0 件は no-op fallback。[hirameki-char-stun.spec.ts](../../tests/e2e/patterns/hirameki-char-stun.spec.ts) fire test を `'sleep'` 検証に upgrade。

### 修正範囲
- ✅ **修正済 (Phase 7-1)**: D08019 a2 + D11009 a3 (hiramekiCharStun 経路、`hiramekiResolve` から発動)
- ⏳ **未修正 (Phase 7-2 で対応予定)**: 他 7 cards (D11020/D11014/D11012/D11005/D11015/D11003/D08003) — これらは triggered ability や declared ability から発動し `hiramekiResolve` 経路を通らないため、汎用的な `entryToCtx` 拡張 + atom-level $pick substitution が必要

## 期待動作 (元)

(以下、元の RCA + 影響範囲 + 関連ファイル の section はそのまま保持)

## 実動作 (現状の no-op fallback)

`hiramekiResolve('fire')` 後、scene char の state は **`'active'` 不変**。E2E (`tests/e2e/patterns/hirameki-char-stun.spec.ts`) で確認。

既存 `tests/integration/hirameki-e2e.test.ts:153-171` (Test 5) も「sceneSetState は対象 0 で no-op、dispatch は ok を返す」と明示コメント、state 変化検証は誰も書いていない (既知挙動)。

## 期待動作

`hiramekiResolve('fire')` 時、`hiramekiCharStun` (effect = `choice → atom sceneSetState (uid:$pick, state:'sleep')`) が resolver で展開され:

1. choice の唯一 option (atom sceneSetState) が選択
2. `target: {kind:'pick', query:{area:'scene', side:'either'}, n:{min:0,max:1}}` で対象 1 件を pick
3. `$pick` placeholder が pick 結果の uid に substitution
4. `mutate.scene.setState(state, side, uid, 'sleep')` で scene char を sleep 化

→ scene char 1 件が sleep 状態になる (rules/10 + factory description)。

## 実動作 (現状の no-op fallback)

`hiramekiResolve('fire')` 後、scene char の state は **`'active'` 不変**。E2E (`tests/e2e/patterns/hirameki-char-stun.spec.ts`) で確認。

既存 `tests/integration/hirameki-e2e.test.ts:153-171` (Test 5) も「sceneSetState は対象 0 で no-op、dispatch は ok を返す」と明示コメント、state 変化検証は誰も書いていない (既知挙動)。

## RCA

- [src/engine/resolve/stack.ts:35-45](../../src/engine/resolve/stack.ts) `entryToCtx`: pendingEffects 解決時の ctx は `{source, bindings:{}, triggerPayload}` のみ、`dyn` field 未供給 → resolver の choice 分岐 (`resolver.ts:46`) は `ctx.dyn?.choiceIndex` 未定義 → `idx=0` 既定動作
- [src/engine/effect/atom-handlers.ts:154-157](../../src/engine/effect/atom-handlers.ts) `sceneSetState`: `a.uid as string` を **リテラル文字列のまま** `mutate.scene.setState` に渡す。`$pick` placeholder の substitution 処理は engine 全体で未実装 (Grep 確認、0 件)
- [src/engine/target/resolve.ts:11-26](../../src/engine/target/resolve.ts): `pick` kind は `picked` 引数必須で auto-resolve しない。pendingEffects 経路で `picked` を供給する仕組みが現状無い
- [src/engine/effect/resolver.ts:12](../../src/engine/effect/resolver.ts) コメント: 「UI からの選択ルーティングは **Phase 7** で実装する」 → known deferred マイルストーン

## 影響範囲

`$pick` を使う atom を持つ effect は本 BUG により全件 no-op fallback。`grep -r '\$pick' src/cards/` で **9 件**:

- `D11020` (個別 sequence step1 sceneRemove $pick)
- `D11014` ($pick sceneRemove)
- `D11012` ($pick sceneRemove)
- `D11005` ($pick sceneRemove via caseTraitConditioned wrapping)
- `D11015` ($pick)
- `D11003` ($pick via caseTraitConditioned)
- `D08003` ($pick)
- `D08019` ($pick via hiramekiCharStun)
- `D11009` ($pick via hiramekiCharStun)

共通基盤の制約のため、本 BUG fix は **9 件一括解消**。

## 関連ファイル

- [src/engine/resolve/stack.ts](../../src/engine/resolve/stack.ts) — `entryToCtx` 拡張対象
- [src/engine/effect/atom-handlers.ts](../../src/engine/effect/atom-handlers.ts) — `$pick` substitution 実装対象
- [src/engine/target/resolve.ts](../../src/engine/target/resolve.ts) — `picked` 供給 API 追加対象
- [src/engine/effect/resolver.ts](../../src/engine/effect/resolver.ts) — Phase 7 deferred コメント
- [src/cards/_shared/hiramekiCharStun.ts](../../src/cards/_shared/hiramekiCharStun.ts) — 直接影響カード factory
- [tests/integration/hirameki-e2e.test.ts](../../tests/integration/hirameki-e2e.test.ts) Test 5 — 既存 no-op fallback の前例
- [tests/e2e/patterns/hirameki-char-stun.spec.ts](../../tests/e2e/patterns/hirameki-char-stun.spec.ts) — 本 BUG の既知挙動 spec

## 修正方針 (Phase 7 で実装予定)

1. `entryToCtx` に `dyn` 注入機構: choice index / pick result を pendingEffect から ctx に注入
2. `$pick` substitution API: atom resolver が ctx.dyn から pick result を取り出して `args.uid` 等を substitution
3. AI policy `chooseHiramekiCharStunTarget` 追加 (opp の hirameki も自動選択)
4. UI 経由 picked 供給: `hiramekiResolve` action に pick choice を含める拡張

修正後、本 spec の fire test を `expect(stateAfter).toBe('sleep')` に書き換えて effect 動作を確認。

## 防止策

- 共通クラスでの `$pick` 使用は Phase 7 完了まで shape verification のみで spec 化 (本 spec のパターン)
- `.claude/specs/card-addition-checklist.md` に「`$pick` を含む atom は Phase 7 fix までは no-op fallback」を追記推奨 (Round 4l+ で)

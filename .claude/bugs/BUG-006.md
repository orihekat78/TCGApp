---
id: BUG-006
title: アクション[事件] で証拠が変動しない
severity: 重大
category: engine
status: 検証中
round: Round 4a
date_found: 2026-05-18
reporter: user
---

## 期待動作

アクション[事件] がガードされなかった場合、相手証拠 -1 / 自証拠 +1 (rules/10)。

## 実動作

ユーザ実プレイで「証拠が動かない」と報告。

## 関連ファイル

- [src/ui/hooks/useEngineDispatch.ts](../../src/ui/hooks/useEngineDispatch.ts) (L274-286 actionJudge case)
- [src/engine/flow/action-case.ts](../../src/engine/flow/action-case.ts) (`removeOpponentEvidenceTop`, `gainSelfEvidence`)
- [src/ui/hooks/useContactFlowDriver.ts](../../src/ui/hooks/useContactFlowDriver.ts) (L215-216)

## RCA

調査結果、useEngineDispatch.ts L277-280 で `ax.target.kind === 'case'` 分岐は **存在し**、engine 関数 `flow.actionCase.removeOpponentEvidenceTop` + `gainSelfEvidence` は呼ばれている。

仮説: state-machine 上で 'judge' phase に到達していない可能性 (guard-window で stuck 等)。Round 4a の Playwright 検証では再現に至らず、状況不明。

## 水平展開

[BUG-006 (本件)] の隣接ケース:
- char-target action は正常 (action-resolution.ts で証明)
- AI policy (one-shot) は正常 (`resolveActionAgainstCase`)
- UI per-step path (`actionDeclareCase` → guard → judge) のみ問題の可能性

## 防止策

end-to-end integration test (`tests/integration/dispatch-to-state.test.ts`) で UI dispatch → engine → state 変化を通しテスト。次セッションで実機 Playwright で actionDeclareCase → judge phase 到達まで観察。

---
id: BUG-163
title: B08079/B08079P ピンガ — henso 列 (col13)【変装】【事件黒】【FILE7】が DSL 未収載 (grounding 漏れ)
severity: 中
category: card
status: 修正済
round: 2026-07-02 Track B B3-2 shipped-gap-suspect certify (compiler mine の「印字行 > ability 数」検出 → opus 敵対 verify で確定)
date_found: 2026-07-02
date_fixed: 2026-07-02
reporter: claude (gap-suspect 27 枚 per-card certify workflow。verify agent が TSV col14 印字 + qAndA「●【変装】」設問で実在を裏取り、refute 失敗 → 確定)
recurrence_cluster: none
related: BUG-117 (B01075/B01089 col12 ヒラメキ漏れ — 同型の印字列 grounding 漏れ), B02038/D06012 (icon-disguise gate exemplar)
commit: (本セッション commit)
---

## 概要

B08079 ピンガ (SR) / B08079P (パラレル) の出荷 (session61 caseColorNot wave) 時、effect 列 3 能力 (a1-a3) のみ
DSL 化され、**henso 列 (col13) の【変装】【事件黒】【FILE7】が丸ごと未収載**だった。DEFER 記録も無し =
silent grounding 漏れ。[[feedback-card-grounding-all-printed-columns]] (col10 だけでなく col11/12/13 も必ず写す)
の違反事例そのもの。

## 期待動作

事件が【黒】を持ち FILE 7 枚以上のとき、コンタクト中に手札の B08079 で変装できる (rules/09/17)。

## 実動作 (修正前)

icon-disguise ability が無いため canDisguise が常に false — このカードで変装が一切できない。

## 修正

両 printing に a4 追加: `type:'icon-disguise'` + `condition: and[{caseColor:'黒'},{fileAtLeast:7}]`
(B02038 a3 / D06012 a1 同型、engine 変更 0)。probe = tests/cards/bug-163-b08079-henso.test.ts
(gate 実評価: 黒+FILE7 可 / 黒なし不可 / FILE6 不可 / 2色{黒,青} 可)。

## RCA

- 出荷 wave (caseColorNot) の関心が a3 (宣言ゲート) に集中し、henso 列の突合が抜けた。
- compiler mine の「印字行 > 実装 ability」検出 (gap-suspect) が本件を機械的に surface — 再発時も同経路で捕捉される。

## 水平展開

gap-suspect 27 枚 (base 20) を全数 opus certify 済 (本 session workflow):
- 同型の**未記録欠落は B08079 と B09100 の 2 件のみ** (B09100 は [[BUG-164]] で別起票、engine 側)。
- 残り 18 枚 = FULL 7 / DEFERRED_DOCUMENTED 11 (意図的 defer)。
- 副産物のガバナンス gap: B03032/B04018 が in-file DEFERRED のみで DEFERRED-INDEX 台帳漏れ → 同 commit で追記。
  B05058 の defer 理由 stale (grantTraits は wave-6 出荷済) → INDEX 注記更新。

## 防止策

- → enforcement: compiler mine gap-suspect 検出 (scripts/compiler/mine.cjs) を新カード投入時に再実行 (CI surface)。
- カード追加時 checklist の「印字全列 ⇔ DSL 突合」を henso/cutin/hirameki 列まで必ず通す (既存規約の徹底)。

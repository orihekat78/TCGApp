---
id: BUG-140
title: 出荷済カード 76 枚で TSV cutIn/hirameki 列のアイコン能力が def から欠落
severity: 高
category: cards
status: 対応中 (commit 後 docs commit で修正済+hash 化)
round: engine拡張 wave#2 cluster2 (2026-06-13)
date_found: 2026-06-13
date_fixed: 2026-06-13
reporter: claude (cluster2 MCP decoy 検証 — B04096 が「カットイン持ち黒カード」filter に
  一致しないことから発見、機械監査で水平展開)
recurrence_cluster: カード実装の列取りこぼし (BUG-114 系の batch 由来)
related: BUG-122 (filter 側は修正済 — 本バグは def 側のデータ欠落)
commit:
---

## 概要

catalog-reuse / 旧 batch 出荷カードの一部が、TSV の **cutIn / hirameki 列を実装せず**
effect 列のみで出荷されていた。機械監査 (`npx tsx scripts/audit-icon-abilities.mts`、
defHasKeyword = 単一真実源で判定) の結果:

- **cutin 欠落 13 枚** / **hirameki 欠落 63 枚** (出荷 1137 枚中、P 版含む)
- 全リスト: [BUG-140-audit-data.txt](BUG-140-audit-data.txt)

## 影響

1. 該当カード自身がコンタクトでカットイン使用不可 / 証拠リムーブ時にヒラメキ発動しない
2. ability-presence filter (「【カットイン】を持つカード」等、cluster2 で解禁) が該当カードを
   正しく検出できない — B03128 の decoy 検証で B04096 が match せず発見
3. 大半は「カードを1枚引く」「証拠を1つ得る」級の定型 hirameki — 修正は機械的だが 76 枚

## 修正状況 (2026-06-13 補修 wave 完了)

- **B04096 / B04096P**: cluster2 で先行補修済 (conditional if/else でターン別 AP+1000/+3000)
- **残 76 行を一括補修** (`scripts/fix-bug140-icon-abilities.mts` 決定論パッチ、dry-run→write):
  - 直接 patch 52 ファイル (h-draw 31 / h-sleep 12 / h-evid 17 / h-removeYellowToHand 2 /
    c-ap1000 3 / c-ap2000 4 / c-turnAP 5 の正準形テンプレ — D03011/D05007/D08015/B04096 同型)
  - spread 再録 22 行は base 補修で自動継承 (TSV テキスト一致を機械検証済)
  - **DEFER 2 枚**: B05039 (contact 対象の特徴条件が Condition union に不在) /
    B06035 (hirameki fire 経路内 chain+条件 gate 未確証) → DEFERRED-INDEX 記載
- 挙動検証: `tests/integration/bug-140-hirameki-batch.test.ts` (4 テンプレ × 実 fire 経路、decoy 付) +
  `tests/engine/flow/bug-140-cutin-batch.test.ts` (3 テンプレ × 実 contact 経路、turn 両分岐)

## 防止策 (実施済)

- `npm run lint:icon-abilities` 新設 (旧 audit script を fail 化 + DEFER allowlist + stale 検知)。
  pre-commit + CI (規約 lint 8 本目) に組込み済
- card-addition-checklist §0 に「TSV cutIn/hirameki/henso 列の非空確認」を追記

---
id: BUG-113
title: 数値ターゲットフィルタの有効値判定が継続効果 (continuousDelta / dyn AP·LP) を含まない
severity: 低
category: engine
status: 対応中
round: 2026-06-04 ルール準拠改善 Task 2 の残差 / 完了 2026-06-07
date_found: 2026-06-04
date_fixed: 2026-06-07
reporter: Task 2 効果値フィルタ修正時の自己レビュー
recurrence_cluster: effective-value-read
related: BUG-095 (continuousModifier.apDelta の engine 配線) / read/char.ts continuousDelta
commit:
---

## 仕様
ターゲット/条件の数値フィルタ (`apMin/apMax/lpMin/lpMax`) は「効果解決時点の有効値」を参照すべき
(rules/15,19,22)。有効値 = `(override?printed) + turnEffects±修正 + continuousDelta(継続効果 dyn)`。

## 実動作
Task 2 で `matchOneFilter` (target/candidates.ts) を turnEffects ±修正込みの有効値判定に修正したが、
`continuousDelta` (read/char.ts の dyn 集計、`continuousModifier.apDelta/lpDelta`) は **import cycle 回避**
のため inline 集計から **除外**。read.char.ap/lp (display 等) は continuousDelta を含むため、継続効果を持つ
キャラでは **filter と display で AP/LP がずれる**残差がある。

### 現状の影響: 軽微
- `continuousModifier.apDelta` を engine 配線で持つカードは **D08005 (灰原哀, evidence-per-AP)** のみ (BUG-095)。
- 灰原哀が「APX以下」リムーブ等の対象になり、かつ証拠数で AP がしきい値をまたぐ場合のみ不整合。稀。

## 恒久対応案 (DEFERRED)
- `read/char.ts` の AP/LP セレクタを cycle 無しに切り出し (continuousDelta も含む純粋関数)、
  candidates.ts から共有する。または candidates.ts に continuousDelta 相当を cycle-safe に inline 化。
- cond/eval ↔ candidates ↔ read.char の依存関係を整理してから行う (cycle に注意)。

## 修正 (2026-06-07, TDD)
`candidates.ts matchOneFilter` の有効 AP/LP に `continuousDelta` を加算。cycle / 無限再帰を以下で回避:
- **静的循環回避 (late-binding)**: `candidates.ts` に `registerContinuousDelta(fn)` を追加し、`read/char.ts` が
  module load 時に自身の `continuousDelta` を注入。`candidates` は `read/char` を import しない
  (transitive 依存 = card-def-registry → read/def, read/keyword は全て leaf) ため新規循環なし。未登録時は 0 (旧挙動)。
- **無限再帰回避 (guard)**: `_inContinuousDelta` フラグで matchOneFilter→continuousDelta→evalCond→sceneHas→
  candidates→matchOneFilter の再入を遮断 (再入時 0)。これが当初省略の本来理由 (cycle 注記の実体)。

## 検証
- TDD RED→GREEN: `tests/engine/target/bug-113-continuous-delta-filter.test.ts` (D08005 表向き証拠3枚で
  有効 AP=9000、apMax:8000 で対象外 / apMin:9000 で対象 / 証拠0枚で 6000 回帰)。
- full vitest **1854 pass / 0 fail** (回帰0)、typecheck clean、eslint 0。
- 水平展開: continuousModifier apDelta/lpDelta を持つカードは **24 枚** (D08005/D09006/B03022/B08007 …) に増加
  (2026-06-04 時点は D08005 のみ)。全 24 で filter=display が一致するようになった。全 card test green。

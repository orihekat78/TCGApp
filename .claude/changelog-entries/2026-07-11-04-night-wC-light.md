## 夜間自走 Wave C 軽 batch — granted-declared + hirameki optional (+5 printings、1948→1953)

- **charGrantAbility declared 解禁 4 点** (B06042+P): atom type honor + grantedId #N 独立 /
  findDeclaredAbility granted 走査 (grantedDeclaredAbilitiesOf helper) / validate declared 免除 /
  UI+AI enumerator 対称走査。granted = bindPick(opp)+startContact (startContact 初 live consumer)。
- **hiramekiResolve humanChooser** (B06032+P / B09081): hirameki 内 optional が常に AI-skip collapse
  していた実バグを修正 — owner=human で optional surface (EffectOptionalModalHost 既存経路で解決)。
  B09081 は Wave 0 DEFER の解禁。
- T2 混成 review: 両 lens CLEAN (NIT 1 = B09081 a2 の「1枚まで」が optional 諾否に折込 —
  B03085/B05111 既存 exemplar 踏襲、候補常に 1 体で実害なし)。
- DEFER: B07001 (cost-removed trait-count dyn 不在) / B09109 (revealFromHand cardName costPaid +
  $cost bindRef 経路不在)。
- gates: tsc 0 / vitest 5349 / smoke 472 exc0 / lint err0 / crosscheck ok。**1953 / 2074 = 残 121**。

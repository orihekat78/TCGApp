# SETUP Screen Refresh Plan

1. Add failing component tests for shared header, actual deck identity,
   provisional side-specific selection, and semantic controls.
2. Extract the approved HOME header into one shared component.
3. Replace fixed-position legacy setup tiles with a responsive versus stage.
4. Preserve session, spectator, first-player, swap, randomize, metadata,
   public fixture, and guarded start behavior.
5. Add compact/desktop layout E2E and migrate structural setup selectors to
   accessible names.
6. Run focused tests, Meta Playwright, typecheck, lint, build, full Vitest,
   generated-doc checks, horizontal investigation, and independent reviews.
